# v9.2.1 状态快照修复

日期：2026-07-31
commit: `fix: 新增 snapshot 只读快照，状态查询不再推进状态机`

## 问题根因

显示层把 `engine.update("")` 当只读查询用，但 `update()` 有副作用：
空输入同样会推进鬼化余韵倒计时、衰减连续负面/拖延计数、写入意图记录。
GUI 每条消息后都刷新状态栏 → 状态机每轮"空转"一次，鬼化余韵衰减翻倍，连续负面被悄悄清零。

## 修改内容

| 文件 | 改动 |
|------|------|
| `shared/state.py` | `HardStateEngine` 新增 `snapshot()`（pure read-only）：复用 `_get_favor_level()` / `_get_ram_stage()`，按 `update()` 末尾相同字段组装 `TwinState`，零写入 |
| `llm/bridge.py` | `status()`：`engine.update("")` → `engine.snapshot()` |
| `gui.py` | `update_status()`：两处 `engine.update("")` → `engine.snapshot()` |
| `CHANGELOG.md` | 新增 v9.2.1 条目 |

未动：`update()` 本体、意图分类、数值规则、PromptBuilder、记忆格式。
`local/rem_ai.py` 的 `set_arc()` 中 `engine.update("")` 是真实同步需求，按计划保留。

## 验证结果

1. 断言脚本全部通过：
   - 触发鬼化后连续 `snapshot()` ×3 → 鬼化阶段/余韵计数不变
   - 制造连续负面后 `snapshot()` ×3 → 计数不变
   - 对照组 `update("")` → 连续负面 1 → 0（证明原 bug 与修复差异）
   - `snapshot().to_prompt_dict()` 字段完整
2. `python main.py --mode local` + `status` ✓
3. `python main.py --mode llm` + `status` ✓（不耗 API）
4. `python gui.py` 启动存活 ✓，状态栏显示正常

## 残留 `update("")` 审计

全仓库仅剩 1 处：`local/rem_ai.py:146`（`set_arc` 内的真实状态同步），属设计内行为，非误用。

## 行为变化说明

修复后连按 `/status`、刷新状态栏不再衰减"连续负面"计数、不再推进鬼化余韵。
真实对话驱动的状态更新节奏完全不变。
