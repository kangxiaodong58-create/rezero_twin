# v10.4.0 EXE 打包与冒烟测试记录

## 项目当前状态

- 当前版本：**V10.4.0**
- Git 最新提交：`8933110`（打包准备：修正 frozen 数据目录 + spec 补全 hiddenimports）
- 上一功能提交：`ecb59d1`（天气确定性修复 + Vignette L0-L3 多级生成）
- 冒烟测试：**15/15 通过**（零 API）

## 打包过程

| 步骤 | 命令/操作 | 结果 |
|------|----------|------|
| 安装 pytest | `python -m pip install pytest` | 成功 |
| 冒烟测试 | `python -m pytest tests/smoke_test.py -v` | **15 passed** |
| 提交打包准备 | `git commit` | `8933110` |
| PyInstaller 打包 | `pyinstaller ReZeroTwin.spec --clean` | 成功 |
| 复制 .env | `Copy-Item .env dist\.env -Force` | 完成 |

### 打包产物

- 输出路径：`dist/ReZeroTwin.exe`
- 体积：**约 69 MB**（PySide6 + openai + PIL 等依赖导致）
- 同级文件：`dist/.env`（含 DeepSeek API Key）
- 运行时数据目录：`dist/data/`（memory.json、conversation.db、vignette_cache.json、gui.log）

### PyInstaller 警告

构建过程中出现一条非致命警告：

```
WARNING: Failed to collect submodules for 'openai.helpers' because importing 'openai.helpers' raised:
MissingDependencyError: missing `numpy`
This feature requires additional dependencies: $ pip install openai[voice_helpers]
```

**影响评估**：`openai.helpers` 是语音/辅助模块，本项目仅使用 `chat.completions`，不影响运行时。无需安装 numpy。

## 运行时目录结构要求

EXE 启动后，数据统一写入 **EXE 同级目录** 的 `data/` 文件夹：

```
dist/
├── ReZeroTwin.exe
├── .env
└── data/
    ├── memory.json          # 世界状态 + 硬状态
    ├── conversation.db      # SQLite 对话流水
    ├── vignette_cache.json  # 开场引言 L0 缓存
    └── gui.log              # GUI 运行日志
```

> 关键修复：frozen 模式下 `_PROJECT_ROOT` 指向 PyInstaller 临时目录（`_MEIPASS`），退出即丢失；v10.4 统一改用 `get_data_dir()`，源码时指向项目根，frozen 时指向 EXE 同级目录。

## 冒烟测试清单

```
tests/smoke_test.py::test_engine_favor_and_risk PASSED
tests/smoke_test.py::test_arc_switch PASSED
tests/smoke_test.py::test_recover_reunion PASSED
tests/smoke_test.py::test_snapshot_no_side_effect PASSED
tests/smoke_test.py::test_memory_store PASSED
tests/smoke_test.py::test_prompt_builder PASSED
tests/smoke_test.py::test_keyword_judgment_v926 PASSED
tests/smoke_test.py::test_amnesia_prompt_v927 PASSED
tests/smoke_test.py::test_event_memory_v930 PASSED
tests/smoke_test.py::test_intent_affirm_v931 PASSED
tests/smoke_test.py::test_local_convergence_v940 PASSED
tests/smoke_test.py::test_value_channels_v950 PASSED
tests/smoke_test.py::test_local_interact PASSED
tests/smoke_test.py::test_world_state_v1040 PASSED        # v10.4 新增
tests/smoke_test.py::test_vignette_v1040 PASSED           # v10.4 新增
```

## 待用户验证

1. 双击 `dist/ReZeroTwin.exe` 是否正常弹出窗口
2. 无历史对话时是否显示开场引言占位/内容
3. 输入消息后 LLM 是否回复（需 `.env` 有效 + 余额充足）
4. 关闭重启后聊天记录是否保留
5. 若有问题，提供 `dist/data/gui.log`

## 二次验证记录（本次会话）

| 检查项 | 命令/操作 | 结果 |
|--------|----------|------|
| 项目版本 | `CHANGELOG.md` 头版本 | **V10.4.0**（2026-08-01） |
| Git 环境 | `git status` / `git log` | 当前 PowerShell 环境无法识别 `git`，研判以文件系统为准 |
| 源码冒烟测试 | `python tests/smoke_test.py`（venv 隔离环境） | **15/15 通过**（零 API） |
| Vignette 降级验证 | 测试中 LLM 路径因无 API Key 三次校验失败 | 正确降级至 L2 动态模板，最终通过 |
| PyInstaller 打包 | `pyinstaller ReZeroTwin.spec --clean --noconfirm` | 成功，产物 `dist/ReZeroTwin.exe` |
| 产物体积 | `Get-ItemProperty` | **约 53 MB**（PySide6 6.11.1 + openai 2.52.0） |
| EXE 启动验证 | `QT_QPA_PLATFORM=offscreen` 启动 8 秒 | 进程持续运行，未崩溃 |
| EXE 启动日志 | `dist/data/gui.log` | 记录到 `gui V10.4.0, frozen-safe data dir`、世界状态加载、历史 30 条、LLM bot 创建成功、窗口 show 完成 |

> 结论：v10.4.0 全部改动已打进 EXE，源码冒烟测试与 EXE 启动验证均通过。

## 按 docx 方案的补充实现（低风险兼容层）

用户后续提供《代码实现》docx，要求按该文件继续实现。经风险评估，完全按 docx 新建独立 `world_state.json` 属于架构级重构（涉及 7+ 文件、存档迁移、回归风险）。经用户确认，采用**低风险补充方案**：

1. 新建 `shared/world_state.py` 作为 docx 兼容层：
   - 暴露 docx 中同名的 `WorldState` 类与函数：`load_world_state` / `save_world_state` / `update_world_state_on_startup` / `mark_interaction`
   - 底层委托给已验证的 `shared.state.WorldState`
   - 提供 docx 字段别名：`last_real_timestamp` / `last_interaction_real` / `days_away` / `system_date` / `hour`
   - **不引入新的 `world_state.json`**，仍然沿用 `memory.json` 单持久化管线

2. 在 `shared/vignette.py` 新增 `prepare_session_opening(llm_callable, ...)` 入口函数，签名与 docx 一致，返回 `(WorldState, vignette)`。

3. `ReZeroTwin.spec` 的 `hiddenimports` 补入 `shared.world_state`。

4. `tests/smoke_test.py` 新增 2 项测试：
   - `test_world_state_docx_compat`：验证字段别名与读写函数
   - `test_prepare_session_opening`：验证 docx 启动入口

### 补充验证结果

| 检查项 | 结果 |
|---|---|
| 源码冒烟测试 | **17/17 通过**（新增 2 项 docx 兼容测试） |
| PyInstaller 打包 | 成功，产物 `dist/ReZeroTwin.exe` |
| EXE 启动验证（offscreen） | 进程持续运行 8 秒，未崩溃 |
| EXE 启动日志 | 正常加载 memory.json、世界状态、历史、LLM bot |

## 已知限制

- EXE 体积约 53 MB，主要为 Qt6 + openai
- 首次冷启动可能比源码运行慢 2~4 秒
- 开场引言生成需要联网 API 调用；失败会自动兜底
- 当前验证环境无图形界面，故使用 `QT_QPA_PLATFORM=offscreen`；用户侧双击运行无需此设置
