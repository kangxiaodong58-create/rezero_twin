# Re:Zero Twin System 技术研判报告

**版本**：V10.4.0（2026-08-01）  
**研判日期**：2026-08-01  
**研判范围**：源码、`CHANGELOG.md`、`README.md`、`docs/architecture.md`、开发日志、测试、`ReZeroTwin.spec`、打包产物  
**验证状态**：`tests/smoke_test.py` 17/17 通过；`dist/ReZeroTwin.exe` 可启动

---

## 一、项目现状总览

### 1.1 当前最高版本与主要能力

当前最高版本为 **V10.4.0**，是一个已可运行、可打包、具备完整 GUI 与 LLM 桥接的角色扮演系统。主要能力如下：

| 维度 | 已实现能力 |
|------|-----------|
| 硬状态机 | 蕾姆好感（0~100，5 级）、忠诚锁定、人格独立度（0.0~1.0）、拉姆好感与五阶段评价、鬼化三阶段 + 余韵、记忆恢复进度、魔女残香 |
| 意图识别 | 10 类意图（倾诉、自我否定、拖延、快速回应、边界试探、正常、危险、后期篇章、提及拉姆、从零开始） |
| 记忆系统 | 结构化画像、长期事件记忆（30 条，钉住机制）、SQLite 对话流水 + FTS5 搜索、JSON 硬状态持久化 |
| 世界状态 | 7 个时段、5 种天气、确定性天气生成（MD5）、≥8h 天气自然推演、真实离线天数、`character_actions` 动作槽位 |
| 开场引言 | L0 会话缓存 + L1 LLM 生成（重试 + 校验）+ L2 动态模板 + L3 静态兜底；View-Only，不进入对话历史 |
| 交互模式 | LLM 桥接（DeepSeek/OpenAI 兼容）+ 本地模板模式；GUI 内可 `/llm` `/local` 切换 |
| GUI | PySide6 宅邸 × VN 融合 UI、三栏布局、流式输出、分角色气泡、历史搜索、樱花动画、状态面板 |
| 打包 | PyInstaller 生成 `dist/ReZeroTwin.exe`（约 53 MB），`.env` 外置 |

### 1.2 已实现的核心模块

- `shared/state.py`：枚举、`HardStateEngine`、`TwinState`、`WorldState`、`StructuredProfile`
- `shared/prompts.py`：`PromptBuilder`、`ResponseLibrary`、`RamAI`
- `shared/memory_store.py`：JSON 持久化
- `shared/conversation_store.py`：SQLite + FTS5
- `shared/vignette.py`：开场引言 L0-L3 生成器
- `shared/world_state.py`：docx 兼容层（V10.4 补充）
- `shared/config.py`：`.env` 加载、数据目录解析
- `llm/bridge.py`：`ReZeroLLMBridge`（含流式）
- `local/rem_ai.py` / `local/twin_system.py`：本地模板蕾姆 + 双子总控
- `gui.py`：PySide6 主界面（约 1300 行）
- `tests/smoke_test.py`：17 项零 API 回归测试

### 1.3 整体完成度自评

**约 70%**。理由：

- 核心状态机、LLM 桥接、GUI、持久化、打包均已跑通，不是原型，是可分发产品。
- 但长期记忆召回仍以规则摘要为主，缺少向量化/RAG；LLM 输出缺少结构化校验；世界事件系统只有天气/时段，缺少可扩展的事件机制；测试覆盖以冒烟为主，缺少边界与集成测试。

### 1.4 可运行性判断

| 场景 | 状态 | 说明 |
|------|------|------|
| 本地开发 | 可运行 | `python main.py --mode local` 或 `python gui.py` |
| LLM 模式开发 | 可运行 | 需 `.env` 配置 `DEEPSEEK_API_KEY` |
| 打包 EXE | 可运行 | `pyinstaller ReZeroTwin.spec --clean` 成功；产物约 53 MB |
| LLM 模式 EXE | 可运行 | 需 `.env` 与 EXE 同目录；无 key 时自动回退本地模式 |

---

## 二、架构评审

### 2.1 分层清晰度

**总体判断：骨干健康，硬软分离原则已落地。**

`HardStateEngine` 拥有全部数值与阶段转换逻辑（好感、独立度、拉姆阶段、鬼化、事件记忆），`PromptBuilder` 只负责把 `TwinState` 翻译成自然语言约束，`LLM` 只负责生成台词。这是架构上最重要的正确决策。

**已验证的解耦证据**：

- `TwinState` 是只读快照，`PromptBuilder.build(state, world, profile)` 不修改状态。
- `ReZeroLLMBridge._build_messages()` 先调用 `engine.update(user_input)` 再构造 prompt；LLM 输出只进入 `history` 和界面，不反写状态。
- `snapshot()` 接口在 V9.2.1 加入，专门解决「查询推进状态机」的 bug。

**仍存在的纠缠点**：

1. `bridge.py` 顶部执行 `load_env()` 并在导入时触发环境变量加载。`gui.py`、`main.py` 也各自调用 `load_env()`，存在重复加载，但暂无功能 bug。
2. `bridge.py` 在模块导入时从 `shared.state` 导入 `HardStateEngine`，而 `shared.prompts` 又从 `shared.state` 导入，形成间接依赖环：`state.py` 不依赖 `prompts.py`，但 `prompts.py` 依赖 `state.py`，`bridge.py` 同时依赖两者。当前无循环导入问题，因为 `state.py` 没有反向引用 `prompts.py`。
3. `shared/world_state.py` 是为兼容 docx 方案而做的薄层，继承自 `shared.state.WorldState` 并添加字段别名。它本身不产生新持久化路径，但增加了 API 表面，长期应评估是否需要保留两套 `WorldState`。

### 2.2 状态管理设计

**真相来源**：

| 状态 | 真源 | 持久化 |
|------|------|--------|
| 蕾姆好感、独立度、锁定、拉姆好感、鬼化、恢复、事件 | `HardStateEngine` 实例 | `memory.json`（经 `MemoryStore`） |
| 世界状态（时段、天气、离线天数、种子、动作） | `WorldState` 实例 | `memory.json` 的 `world_state` 字段 |
| 对话历史 | `ConversationStore`（SQLite） | `data/conversations.db` |
| LLM 上下文窗口 | `ReZeroLLMBridge.history`（内存） | **未持久化** |
| 开场引言缓存 | `VignetteGenerator._persistent_cache` + `vignette_cache.json` | `data/vignette_cache.json` |

**评价**：

- 硬状态的真源单一且明确，`gui.py` 与 `main.py` 都从 `MemoryStore` 加载并写回同一文件。
- `snapshot()` / `update()` 职责已分离：`snapshot()` 纯只读，`update()` 推进回合。
- **LLM `history` 未持久化**是显著缺口：重启后 LLM 丢失近期对话上下文，虽然 `ConversationStore` 保存了完整历史，但 `bridge.history` 不会从 SQLite 恢复。

**持久化可靠性**：

- `MemoryStore` 使用 JSON，简单有效；旧存档兼容通过 `setdefault` 实现。
- `get_data_dir()` 在 frozen 模式下指向 EXE 同级 `data/`，不可写时兜底 `%APPDATA%\ReZeroTwin\data`，解决了 EXE 失忆问题。
- `world_state` 与硬状态同文件保存，避免了独立 `world_state.json` 的双文件同步风险。

### 2.3 扩展性

**新增世界事件**：当前 `WorldState.active_event` 字段已存在，但代码中没有任何地方设置它。要新增事件，需要修改 `WorldState` 和 `gui.py` 的保存/加载逻辑，扩展成本中等。

**新增角色状态**：需要修改 `HardStateEngine`、`TwinState`、`PromptBuilder`，并在 `memory_store.py` 默认值中添加字段。由于 `PromptBuilder.build()` 是一个大静态方法，新增状态会进一步膨胀 prompt 字符串拼接。

**PromptBuilder 膨胀风险**：`PromptBuilder.build()` 已超过 110 行，硬编码了世界观、角色指令、格式约束、事件记忆、世界状态等所有小节。随着状态增多，这个方法会成为「上帝方法」，维护难度递增。

### 2.4 模块边界

**优点**：

- `shared/`、`llm/`、`local/`、`tests/` 目录职责清晰。
- `ConversationStore` 与 `MemoryStore` 分离，符合「对话流水」与「硬状态」不同生命周期的设计。

**风险点**：

- `gui.py` 当前约 1300 行，承担了 UI 构建、事件处理、网络线程管理、状态持久化、模式切换、开场引言生成、历史搜索等多项职责。它是一个事实上的上帝类，长期应拆分出 `ChatPresenter` 或 `SessionController`。
- `RamAI` 在 V9.4 已将好感读写统一至 `engine.ram_favor`，但 `_stage` 仍是内部字段，由 `on_rem_treated_well` / `on_rem_hurt` 更新。`HardStateEngine._get_ram_stage()` 与 `RamAI._update_stage()` 存在重复阈值逻辑，存在漂移风险。

---

## 三、核心子系统专项审查

### 3.1 硬状态机（HardStateEngine / TwinState）

**现状**：

`HardStateEngine` 是项目最成熟的部分。状态转换规则集中、阈值明确、事件检测自动化。

**问题**：

1. **查询污染已修复但仍有潜在风险**：`snapshot()` 不推进状态机，但 `HardStateEngine` 内部仍有 `turn_count` 等字段，任何新增方法如果不注意只读语义，仍可能引入查询副作用。
2. **鬼化余韵逻辑在引擎中正确，但在本地模式历史代码痕迹已清除**：V9.4 修复了 `RemAI._oni_aftermath` 死代码，目前由引擎统一管理。
3. **拉姆阶段双源风险**：`HardStateEngine._get_ram_stage()` 与 `RamAI._update_stage()` 使用完全相同的阈值（85/66/46/25）。当前行为一致，但任何一方修改而另一方未同步都会导致显示与判定不一致。
4. **`breaker_triggered` 只能通过 `mark_breaker_triggered()` 设置**：本地模式在 `RemAI.generate()` 中按概率触发，LLM 模式目前不会触发破局者彩蛋——这是设计选择还是遗漏需确认。

**优化建议**：

- 将拉姆阶段计算统一为 `engine.ram_stage` 字段，由 `HardStateEngine.update()` 末尾统一更新并写入 `TwinState`，`RamAI` 只读取不计算。
- 为 `HardStateEngine` 增加只读断言或单元测试，确保 `snapshot()` 不修改任何内部计数器。

### 3.2 记忆与上下文系统

**现状**：

三级记忆映射已在 V10.2 成型：

- L1 结构化画像（`StructuredProfile`，约 150 token）：从引擎提取的名字、关键里程碑、重要承诺。
- L2 长期事件记忆（`events`，约 200 token）：钉住 + 最近事件至多 6 条注入 prompt。
- L3 滑动窗口（`bridge.history` 最近 8 轮，约 800 token）。

**问题**：

1. **LLM 上下文不持久**：`ReZeroLLMBridge.history` 仅存内存，程序重启后丢失，导致 LLM 无法延续多轮对话上下文。`ConversationStore` 虽有完整历史，但当前未用于恢复 `bridge.history`。
2. **长期事件召回依赖规则摘要**：事件按「钉住 + 最近」选取，缺少语义相似度召回。例如用户问「还记得我们第一次正式说话吗」，如果该事件不是最近 6 条且未被钉住，LLM 可能看不到。
3. **结构化画像未持久化独立存在**：`StructuredProfile.from_engine()` 每次从引擎现场生成，没有自己的持久化或版本，虽然逻辑上等同于引擎快照，但缺少长期关系演化的「记忆卡片」。

**优化建议**：

- 启动时从 `ConversationStore.get_recent(limit=8)` 恢复 `bridge.history`，让 LLM 模式重启后保留上下文。
- 对长期事件引入轻量语义检索（如 SQLite `fts5` 已可用，或小型嵌入模型），提升非钉住事件的召回质量。

### 3.3 世界状态与开场引言（World State + Vignette）

**现状**：

V10.4 是这一子系统的重大改进：

- 天气确定性由 `hash()` 改为 `hashlib.md5`，跨进程稳定。
- `days_since_last` 由 `last_interaction_ts` 真实计算。
- Vignette L0-L3 多级生成网络设计合理，零 API 成本缓存 + LLM 生成 + 动态模板 + 静态兜底。

**问题**：

1. **`WorldState.active_event` 是死字段**：字段存在但没有任何代码写入，prompt 中「最近事件：无特殊事件」永远不变。
2. **`mark_interaction()` 仅在 `gui.py` 触发**：`main.py` 的 CLI 模式和 `bridge.py` 的 `chat()` 不会更新 `last_interaction_ts`。如果用户长期用 CLI，离线天数计算会失效。
3. **Vignette 缓存分桶较粗**：按「时段 | 天气 | 离开天数桶（0/1-2/3+）| 蕾姆等级 | 拉姆阶段」分桶，同一桶内不同动作组合会复用同一段引言，长期可能显得重复。
4. **引言与后续对话的氛围衔接较弱**：引言是独立 View-Only 数据，LLM 的 system prompt 中虽然有世界状态小节，但引言本身不会作为上下文传入 LLM。这意味着「开场氛围」与「第一轮回复」之间缺少显式承接。

**优化建议**：

- 在 `HardStateEngine` 或 `gui.py` 中为世界事件系统提供可扩展入口，例如每日首次启动时根据日期/季节设置 `active_event`。
- 在 `bridge.chat()` 和 `main.py` 的输入处理中也调用 `mark_interaction()`，确保 CLI 与 LLM 模式一致性。
- 考虑将 Vignette 文本作为「系统氛围提示」注入第一轮 LLM prompt（可选开关），增强氛围延续。

### 3.4 LLM Bridge 与 Prompt 工程

**现状**：

- `PromptBuilder.build()` 把硬状态翻译为结构清晰的中文 system prompt，包含当前硬性状态、角色要求、输出格式、共同经历、世界状态。
- 温度固定 0.65，max_tokens 600，max_history 8。
- Vignette 生成使用 `raw_completion()` 绕过角色 system prompt，避免污染。

**问题**：

1. **缺少 post-generation 校验**：LLM 输出只经过 `_parse_twin_reply()` 按 `【蕾姆】`/`【拉姆】` 拆分，不校验是否包含违禁词、是否遵守第三人称、是否擅自改变状态。
2. **System Prompt 过长且静态拼接**：所有小节在一个 f-string 中硬编码，新增状态需要修改多段字符串，容易遗漏。
3. **双子分工在 prompt 层有描述但无强制结构**：prompt 中说明了拉姆应在危险/自我否定场景先开口，但 LLM 是否遵守取决于模型能力，没有输出格式强制（例如强制先输出拉姆再输出蕾姆）。
4. **`raw_completion()` 错误回包处理脆弱**：当 API 异常时返回 `（{e}）`，`vignette.py` 已能识别括号包裹的错误回包，但 `chat()`/`chat_stream()` 没有类似清洗。

**优化建议**：

- 增加 `ResponseValidator`：检查第三人称自称、状态词是否被篡改、双子格式是否符合预期、是否出现 OOC 词汇。
- 将 `PromptBuilder` 拆分为多个小构建器（`StatusSectionBuilder`、`EventSectionBuilder`、`WorldSectionBuilder` 等），降低方法膨胀风险。
- 对关键输出增加 JSON/结构化约束，例如要求 LLM 按 `{"rem": "...", "ram": "..."}` 输出，再渲染为气泡。

### 3.5 角色还原与原著契合度（技术视角）

**现状**：

- 系统具备「关系结构」而非单纯好感数值：蕾姆有独立度、拉姆有评价阶段、高好感有忠诚锁定。
- 支持从「替代品」到「独立人格」的演化：独立度由身份肯定句驱动，影响台词选择。
- 支持从「观察」到「托付」的拉姆演化：拉姆阶段由提及/表扬驱动，高阶段触发托付语义。
- 帝国篇失忆有专门的 recovery 机制和 prompt 防备指令。

**问题**：

1. **价值观守底依赖 prompt 文本**：没有独立的「红队过滤器」或价值观护栏。如果 LLM 输出背叛罗兹瓦尔、过度讨好用户等 OOC 内容，仅靠 prompt 约束。
2. **后期篇章锚点较少**：`LATE_ARC` 只在意图识别中通过关键词触发，缺少独立的剧情事件系统。
3. **「托付」语义主要在本地模板中强制**：`RamAI.generate_entrustment()` 能输出固定托付台词，但 LLM 模式下是否体现托付依赖 prompt 描述和模型遵循能力。

**优化建议**：

- 增加独立的红队/价值观校验层，对 LLM 输出进行后验检查，拦截违背原著价值观的回复。
- 在 prompt 中明确罗兹瓦尔、爱蜜莉雅等关键关系的边界，并在测试案例中增加红队用例。

---

## 四、技术债务与风险清单

### P0（必须尽快修）

| # | 现象 | 根因 | 潜在后果 | 建议修复方向 |
|---|------|------|----------|--------------|
| P0-1 | `mark_interaction()` 仅在 GUI 触发 | CLI 和 LLM Bridge 未接入世界状态互动刷新 | CLI 用户看到的「离线天数」永远不准确；LLM 模式重启后 days_since_last 不更新 | 在 `bridge.chat()` 和 `main.py` 的输入循环中统一调用世界状态刷新 |
| P0-2 | `WorldState.active_event` 无写入 | 世界事件系统只有字段，没有事件源 | 开场引言的「最近事件」永远为空；世界状态缺乏生命力 | 增加可扩展的世界事件注册机制，至少每日/季节事件能自动注入 |

### P1（强烈建议下一迭代处理）

| # | 现象 | 根因 | 潜在后果 | 建议修复方向 |
|---|------|------|----------|--------------|
| P1-1 | LLM 对话上下文重启丢失 | `bridge.history` 仅存内存 | 用户重启后前几轮对话缺乏上下文延续感 | 启动时从 `ConversationStore` 恢复最近 8 轮到 `bridge.history` |
| P1-2 | PromptBuilder 方法过大且硬编码 | 所有 prompt 小节在一个静态方法中拼接 | 新增状态易遗漏、维护困难、难以单元测试 | 拆分为多个 SectionBuilder，每个小节独立构建和测试 |
| P1-3 | LLM 输出缺少结构化校验 | 仅按 `【角色】` 拆分，不校验内容合规性 | 模型偶发越界（自称「我」、篡改状态、OOC）无法自动拦截 | 增加 `ResponseValidator`，对第三人称、状态词、OOC 词进行后验检查 |
| P1-4 | RamAI 阶段与引擎阶段双源 | `RamAI._stage` 内部维护，阈值逻辑与引擎重复 | 修改阈值时容易不同步，导致本地模式显示与判定不一致 | 拉姆阶段由引擎计算并写入 `TwinState`，`RamAI` 只读 |

### P2（可规划优化）

| # | 现象 | 根因 | 潜在后果 | 建议修复方向 |
|---|------|------|----------|--------------|
| P2-1 | GUI 文件过大 | `gui.py` 1300+ 行，承担过多职责 | 新功能难以添加，测试困难 | 拆分出 Presenter/Controller 层 |
| P2-2 | 测试以冒烟为主 | 缺少边界、集成、LLM 输出校验测试 | 回归时难以发现细微退化 | 补充单元测试，尤其是状态机边界和 prompt 构建 |
| P2-3 | 长期事件召回依赖时间序 | 缺少语义检索 | 用户询问非近期重要事件时，LLM 看不到 | 引入轻量向量检索或 FTS5 语义召回 |
| P2-4 | `shared/world_state.py` 与 `shared.state.WorldState` 并存 | docx 兼容层增加了 API 表面 | 长期维护两套字段名容易混淆 | 评估是否逐步收敛到单一 `WorldState` 类 |

---

## 五、优化方向与思路（按性价比排序）

### 1. 架构层面：单一真相来源与接口净化

**思路**：

- 将拉姆阶段计算完全收归 `HardStateEngine`，`TwinState` 中增加 `ram_stage` 字段（已有），`RamAI` 只读取 `engine._get_ram_stage()`。
- 将 `mark_interaction()` 和世界状态刷新统一封装为 `SessionFacade`，供 GUI、CLI、Bridge 共用。
- 收敛 `WorldState` API：评估 `shared/world_state.py` 是否可合并回 `shared.state`，或明确 docx 兼容层的生命周期。

**预期收益**：消除双源风险，降低接口复杂度。  
**改动范围**：中等（`state.py`、`prompts.py`、`gui.py`、`main.py`、`bridge.py`）。  
**风险点**：需确保旧存档兼容。

### 2. 状态与持久化：LLM 上下文恢复

**思路**：

- `ReZeroLLMBridge.__init__()` 增加 `conversation_store` 参数，启动时读取最近 8 轮对话并填充 `self.history`。
- 保存时 `chat()` / `chat_stream()` 同时将 user/assistant 消息写入 `ConversationStore`（当前 `gui.py` 已保存显示消息，但 `bridge.history` 与 `ConversationStore` 仍不同步）。

**预期收益**：重启后对话连续性显著提升。  
**改动范围**：中小（`bridge.py`、`gui.py`）。  
**风险点**：历史消息可能包含旧 system prompt，需确保只恢复 user/assistant 角色。

### 3. 记忆系统：长期事件语义召回

**思路**：

- 利用已有 `messages_fts` 虚拟表，对长期事件摘要做 FTS5 检索；用户输入时提取关键词，召回相关事件补充到 prompt。
- 或引入轻量嵌入模型（如 `sentence-transformers`），但会增加依赖，违背当前「不引入新依赖」约束。

**预期收益**：提升非近期重要事件的召回质量。  
**改动范围**：中（`conversation_store.py`、`prompts.py`）。  
**风险点**：FTS5 语义召回精度有限，需控制注入数量。

### 4. Prompt 与生成稳定性：响应校验

**思路**：

- 增加 `ResponseValidator` 类，检查：
  - 第三人称自称（禁止「我」出现在蕾姆/拉姆台词中）
  - OOC 词（玩家、系统、AI、大模型）
  - 状态词篡改（好感、独立度数值是否被 LLM 修改）
  - 双子格式合规
- 校验失败时触发重试或 fallback 模板。

**预期收益**：降低 LLM 偶发越界风险。  
**改动范围**：中小（新增 `shared/validators.py`，修改 `bridge.py`）。  
**风险点**：校验规则需精细，避免误杀正常表达。

### 5. 可观测性与调试：状态透明

**思路**：

- 在 GUI 状态栏或新增「状态详情」弹窗中展示完整 `TwinState` 和 `WorldState`，方便调试。
- 增加结构化日志（JSON 格式），记录每次 `update()` 的输入、意图、状态变化。

**预期收益**：问题定位更快，红队测试更方便。  
**改动范围**：小。  
**风险点**：日志可能包含用户输入，注意隐私。

### 6. 性能与资源：Token 控制

**思路**：

- 评估 prompt 长度，对 `events` 和 `history` 做更严格的 token 预算控制。
- 对 Vignette 缓存命中场景完全避免 API 调用（当前已实现 L0）。

**预期收益**：降低 API 成本，提升响应速度。  
**改动范围**：小。  
**风险点**：过度截断可能影响上下文质量。

### 7. 扩展性预埋：世界事件系统

**思路**：

- 增加 `WorldEventRegistry`：按日期、季节、用户行为注册事件，例如「花园的花开了」「今夜有流星雨」「宅邸大扫除日」。
- 事件自动写入 `WorldState.active_event`，并进入 Vignette prompt 和开场引言。

**预期收益**：世界状态从「有天气」升级为「有生命感」。  
**改动范围**：中（`state.py`、`vignette.py`、`gui.py`）。  
**风险点**：事件过多会稀释重点，需控制频率。

---

## 六、下一阶段建议路线图

### 方向 A：补齐 LLM 上下文恢复（推荐优先）

- **核心目标**：解决 LLM 模式重启后丢失近期对话上下文的问题。
- **主要改动点**：
  - `ReZeroLLMBridge` 启动时从 `ConversationStore` 恢复最近 8 轮。
  - 统一保存 user/assistant 消息到 `ConversationStore`。
  - 在 `main.py` CLI 模式中也接入 `ConversationStore`。
- **验收标准**：重启 GUI/CLI 后，LLM 能引用上一轮对话内容；冒烟测试新增上下文恢复用例。
- **预估复杂度**：中低。

### 方向 B：LLM 输出后验校验

- **核心目标**：降低 LLM 偶发越界风险。
- **主要改动点**：
  - 新增 `ResponseValidator`。
  - 在 `bridge.chat()` / `chat_stream()` 后调用校验，失败时重试或 fallback。
  - 增加红队测试用例到 `docs/evaluation/test_cases.md`。
- **验收标准**：对 10 组红队输入，LLM 越界率低于 5%；校验器不误杀正常回复。
- **预估复杂度**：中。

### 方向 C：PromptBuilder 拆分与世界事件系统

- **核心目标**：提升长期可维护性，让世界状态更有生命感。
- **主要改动点**：
  - 将 `PromptBuilder.build()` 拆分为多个 SectionBuilder。
  - 增加 `WorldEventRegistry` 和 `WorldState.active_event` 写入机制。
  - Vignette 和 Prompt 自动读取 `active_event`。
- **验收标准**：新增世界事件只需修改注册表和 one-line 事件描述；冒烟测试覆盖事件注入。
- **预估复杂度**：中高。

**推荐优先级**：A > B > C。原因：A 直接影响用户体验且改动范围可控；B 是稳定性底线；C 是长期架构健康投资。

---

## 七、信息缺口与假设

| 缺口 | 影响 | 假设前提 |
|------|------|----------|
| 未实际调用 LLM API 进行红队测试 | 无法量化 LLM 越界率、人设稳定性 | 假设当前 prompt 在 DeepSeek `deepseek-chat` 上表现与文档描述一致；建议后续用真实 API 跑 `docs/evaluation/test_cases.md` |
| 未审查 `data/memory.json` 实际存档 | 无法判断用户历史数据是否健康 | 假设当前数据目录中的存档为测试数据，不包含需要迁移的异常状态 |
| 未获取 docx 方案原文 | 无法判断 `shared/world_state.py` 是否完全覆盖 docx 预期 | 假设当前兼容层已实现 docx 所需的同名函数与字段别名；如 docx 还有未实现接口，需补充 |
| 未进行长时间运行压力测试 | 无法确认内存泄漏、widget 泄漏是否彻底解决 | 假设 `MAX_VISIBLE_WIDGETS = 80` 和 SQLite WAL 模式已足够应对典型会话长度 |
| 未审查 `.env` 与密钥实际配置 | 安全判断基于代码层面 | 假设 `.env` 未进入 git 历史，且用户按 README 将 `.env` 置于 EXE 同级目录 |

---

## 八、总体技术健康度评分

**7.2 / 10**

**一句话总结**：Re:Zero Twin System 已完成从原型到可分发产品的跨越，硬软分离架构健康、状态机成熟、打包稳定，但 LLM 上下文持久化、输出校验、世界事件扩展性仍是下一阶段需要补齐的关键短板。
