# Re:Zero Twin System 技术研判报告

**版本基线**：V14.8 + Forensic Kernel M3（2026-08-19）
**研判日期**：2026-08-29
**研判范围**：全量源码、`CHANGELOG.md`、`docs/`（design/devlog/evaluation/audit）、测试套件实测、git 状态、打包产物
**验证状态**：`pytest tests/` **167 passed**（11.25s，零 API，含 33 个预期线程警告）；`dist/ReZeroTwin.exe` 53.7MB（08-19 打包，`dist/data/` 有 08-26 真机运行痕迹）
**前序报告**：`技术研判报告_V10.4.0.md`（08-01，彼时完成度自评 70%）

---

## 一、项目现状总览

### 1.1 一个月演进速览（V10.4 → V14.8）

自上份研判（08-01）以来，项目完成了三个阶段的跨越：

| 阶段 | 版本带 | 核心事件 |
|------|--------|----------|
| 视觉与交互成熟 | V12.0~V14.3 | 宅邸 VN 融合 UI 定稿、消息删除/撤回/引用回复/搜索高亮、双子主动来信系统 |
| 体验攻坚（08-19 单日高峰） | V14.4~V14.8 | **本地模板模式整体退场（LLM 优先路线确立）**、审判循环 Trial #1~#5、原著锚定文案包（角色卡+世界观词汇+软 OOC 五级检查）、场景化三件套（空间场景/名场面/关键人物）、篇章均衡扩池（schema 2.0 加 arc 维度）、EXE 重新打包 |
| 质量基建新轨道 | Forensic M1~M3 | 事件黑匣子 + 崩溃自动取证 + Handoff 交接 + Headless 复现通道（崩溃率可测量），独立于版本号演进 |

### 1.2 当前能力面

| 维度 | 现状 |
|------|------|
| 硬状态机 | 蕾姆好感/忠诚锁定、独立度、拉姆五阶段、鬼化三阶段+余韵、帝国恢复进度、长期事件记忆、软 OOC 五级检查（V14.6） |
| 篇章体系 | 宅邸/帝国/后期 三 arc 贯穿全部内容子系统（来信 58 条、场景 13 个 67 组视角、EVENT_POOL 33 条、registry 69 条） |
| 场景体验 | 空间场景系统（用户可主动「去厨房」）、名场面 5 类联动、关键人物互动 |
| GUI | PySide6 宅邸 VN UI，流式/撤回/引用/搜索/来信/场景漫游；「距下一阶段提示」进度可视化 |
| 测试 | **167 项零 API 回归** + LLM 真机审判循环（Trial #1~#5 全记录，单次成本 ¥0.04~0.15） |
| 取证 | Forensic 黑匣子/案件 manifest/Headless 压测（CLI 侧已接，**GUI 侧未接**，见风险 R1） |
| 打包 | PyInstaller 56MB EXE，build.ps1 含数据安全红线，08-19 真机冒烟通过 |

### 1.3 完成度自评：约 82%（V10.4 时为 70%）

**已不是原型，是带质量体系的可分发产品**：核心体验闭环（对话/来信/场景/篇章）全部真机验收；测试金字塔 L0（167 项 pytest）→ L1（headless 压测）→ L2（真机审判）已成型。

距「完成」仍缺四块：①**回归门禁**（审判循环 Phase 2 未建，体验无防退化机制）；②**视觉表现力**（立绘仍是占位符，评审认定最大视觉缺口）；③**EXE 侧取证**（最需要黑匣子的分发场景反而没接入）；④**长期记忆语义召回**（README 遗留，仍以规则注入为主）。

---

## 二、架构与代码质量研判

### 2.1 分层判断：骨干健康，一处污点

依赖方向清晰：`shared`（纯领域，无 Qt/openai 依赖）← `llm`（唯一触碰 openai SDK 的模块，含 forensic 埋点）← `gui`/`main`；`runtime/forensic` 独立无 PySide6。硬软分离原则（状态机管「真」、LLM 管「美」）持续贯彻。

污点：`shared/state.py:367 ↔ shared/vignette.py` 存在懒加载循环依赖；`gui.py` 顶层读环境变量求值 `_VIGNETTE_DISABLED`，导入顺序决定行为（已逼出 conftest.py 全局补丁，属设计缺陷而非测试问题）。

### 2.2 技术债台账（按严重度）

| # | 债项 | 证据 | 影响 |
|---|------|------|------|
| D1 | **gui.py God File：3422 行**，TwinChatApp 独占 ~1832 行/57 方法，`_setup_ui` 单方法 240 行；领域解析器 `_streaming_segments`/`parse_twin_segments`（内层循环逐行相同）困在 UI 层 | gui.py:90-530, 1591-3422 | 测试只能整窗构造；状态机规则改动需双处同步 |
| D2 | **EXE 入口未接 Forensic**：`runtime/forensic` 的设计目标用户是「分发 EXE 的最终用户」，但 gui.py 零引用；且 `ReZeroTwin.spec` hiddenimports 未列 runtime.forensic，全靠 bridge.py 顶层 import 被动跟随，一旦改懒加载即从包里消失 | gui.py grep=0；ReZeroTwin.spec | M1~M3 投入未触达真实崩溃现场（详见 R1） |
| D3 | 解析器成对重复 + 搜索逻辑两套（HistoryOverlay._do_search 与 TwinChatApp._do_search） | gui.py:422/479/1534/2228 | 维护双份 |
| D4 | 宽泛异常吞噬：`except Exception` 46 处、裸 `pass` 吞异常 10 处 | gui.py 全文 | 静默故障难排查（与取证轨道目标相悖） |
| D5 | EVENT_POOL 约 140 行文案硬编码在 state.py:111-250，未随 V14.4 注册表化方向迁移 | state.py | 内容与引擎耦合 |
| D6 | 无 CI / 无 lint / 无 pytest 配置文件；测试文件「pytest 收集 + 直跑脚本」双形态靠 sys.path 注入维系 | 仓库根 | 质量全靠流程自觉 |

### 2.3 测试体系：167 项全绿，实测通过

本次研判实测 `venv/Scripts/python.exe -m pytest tests/ -q`：**167 passed, 11.25s**。覆盖引擎回归、消息状态、长会话稳定性（30 轮有界性/5000 次事件池压测）、剧情契约、注册表确定性 hash、来信冷却红线、LLM 四类异常兜底、Forensic 三件套（22 用例：并发安全/同 seed 可复现/timeout 注入崩溃率==INC 数）、Qt 离屏 UI。

新增未提交的 `tests/conftest.py` 修复了一起真实事故：pytest 按字母序收集时 gui 模块的禁用标志在 import 时求值过早，导致所有测试窗口写真实 `data/conversations.db`。**该文件必须随下一批提交入库**，否则事故复发。

---

## 三、产品/体验侧研判

### 3.1 审判循环台账：实质仅 1 项 open

Trial #1~#5 累计约 17 个编号：S 级 2 条全修复，A 级 5 条中 4 条修复、1 条因 local 退场移除，B 级 3 条全修复，观察项中仅 **O-5（名场面「从零开始」触发冷却确认，P2）** 真正未关闭。

**台账卫生问题**：`trial1_2026-08-19/ISSUE_TRACKER.md` 底部 4 个 checkbox（Phase 4/B-01/B-02/引用隔离）实际已完成但未勾选；O-1~O-5/G-1/H-1 的闭环记录散落在各 trial 报告中未回填主台账。建议一次专项回填。

### 3.2 内容侧：篇章失衡已闭环，余量集中在宅邸

V14.8 完成三 arc 均衡（场景 13 个/72 句、来信 43→58 条）。遗留：宅邸 7 场景时段变体未扩（V14.8 刻意不动防回归）、厨房上午 slot 仅 2 条轮转明显、registry Step 5「偶发一句 ambient_remark」未做。

### 3.3 体验侧最大缺口：立绘

`体验设计审查_2026-08-19.md` 认定的最大视觉缺口——立绘支持仍为占位「拖入 PNG」，素材组未启动。次要缺口：气泡最大宽度上限（P1）。

### 3.4 长会话压测（未提交成果）结论良好，含一处仪器失真

`docs/evaluation/sessions/long_session_2026-08-19/`（30 轮真实 API）：30/30 成功、零兜底零 OOC、¥0.144、第 15 轮断线重建 bridge 成功、6 次场景切换注入正确。两处需复核：① report JSON 中 `final_scene=GARDEN` 取自断线恢复前旧对象，与第 28 轮切餐厅矛盾（仪器失真，非产品 bug）；② 真机 `history_len=30` 与 L0 测试断言 `history<=8` 口径不一致。

---

## 四、Forensic Kernel 研判

M1+M2（黑匣子/取证/Handoff）与 M3（Headless 复现通道）已提交，实现质量好：环形缓冲双上限（200 事件/60s）、(startup_id, seq) 跨进程分段、双时钟、Windows Defender rename 退避、manifest claim 防双 Agent 认领、commit 实证「注入超时 73% 崩溃率、73 个 INC 全落盘」。22 项专项测试覆盖并发安全与同 seed 可复现。

**与设计的两处实质偏差**：

1. **stale callback 由「拦截」降级为「观测」**：设计要求记录后拒绝执行（design L94-98），实现只记 `STALE_CALLBACK_OBSERVED` 不拦截（bridge.py:556-562）。预防性目标未达成，建议在 M4 一并决策：拦截 or 维持观测并改设计文档。
2. **state_trace/transition() 无人调用**：HardStateEngine/memory_store 的状态轨迹接入点（design L110-124）落空，黑匣子目前只见 LLM 事件流、不见状态机轨迹，崩溃时无法回放数值变化。

**M4 待办**：案件目录模板 + Agent 侧 skill 协议对接，验收 = 首个真实案件走通 CASE_OPEN→CASE_CLOSED。注意设计引用的 `agent_os/FORENSIC_DEBUGGING_PROTOCOL.md` 不在仓库中，M4 前需先落协议文档；案件状态机持久化也推迟至 M4。

---

## 五、风险台账（按优先级）

| 级 | 风险 | 说明 |
|----|------|------|
| R1 [高] | **EXE 无取证** | 打包入口是 gui.py， forensic 未接入——概率性崩溃的「案发现场」恰好是无黑匣子的分发版。连带 spec hiddenimports 隐性脆弱（D2） |
| R2 [高] | **57 个提交未推送** | 含整个 Forensic 系列与 V14.5~14.8，本地单点故障即丢一个月工作 |
| R3 [高] | **成果未提交** | conftest.py（真实事故修复）、test_long_session_stability.py、long_session 评估报告仍在工作区，任何人 reset 即丢失 |
| R4 [中] | **git 卫生** | `data/gui.log` 被跟踪且 +2794 行未提交 diff（.gitignore 漏了它）；根目录 `x.db` 为游散 SQLite（代码零引用，应删）；.gitignore 未列 `venv/`、`Temp/`（现靠工具自动生成的内嵌 .gitignore 兜底） |
| R5 [中] | **无回归门禁** | 审判循环 Phase 2 未完成：`tools/persona_fingerprint.py`、`tools/trial_gate.py`、baselines/ 均不存在。V13→V14 式体验退化无机器防线 |
| R6 [中] | **dist/.env 含真实 API Key** | build.ps1 按设计部署密钥到 dist/，EXE 交付时分发面风险；dist/ 虽已 gitignore，交付给他人时需注意 |
| R7 [低] | stale 拦截降级、transition 未接入（见四）；长会话口径不一致（见 3.4）；无 CI/lint | 列入对应里程碑处理 |

---

## 六、下一步路线建议

### 第一批：落库与卫生（半天，全部低成本高确定性）

1. 提交三件未入库成果：`tests/conftest.py` + `tests/test_long_session_stability.py` + `tests/test_ui_offscreen.py`（+6 行隔离修复）+ `docs/evaluation/sessions/long_session_2026-08-19/`（提交前剔除其中混入的 `__pycache__/`）。
2. git 卫生：`git rm --cached data/gui.log`，.gitignore 追加 `data/*.log`、`venv/`、`Temp/`、`/x.db`；删除根目录 `x.db`。
3. **push 全部 57 个提交**（R2）。
4. 回填 ISSUE_TRACKER：勾选已完成待办、O-1~O-5/G-1/H-1 闭环记录入主台账。

### 第二批：Forensic 收口（M4 + 接入真实现场）

5. **gui.py 入口接入 init_forensic + UI 事件埋点**，`ReZeroTwin.spec` hiddenimports 显式补 `runtime.forensic`（修 R1/D2）。
6. 决策 stale 拦截 vs 观测，回写设计文档；给 HardStateEngine/memory_store 接 `transition()` 状态轨迹。
7. 落 `FORENSIC_DEBUGGING_PROTOCOL.md` 协议文档 → M4 案件目录模板 → 用一个真实 INC 走通 CASE_OPEN→CASE_CLOSED 验收。

### 第三批：回归门禁（审判循环 Phase 2，防体验退化）

8. `tools/persona_fingerprint.py`（8 项指纹指标）+ `tools/trial_gate.py` 版本门禁 + `docs/evaluation/baselines/` 基线入库。
9. 自测「故意引入角色漂移被门禁拦截」→ V15.0 走一次完整审判循环（Phase 3 验收）。

### 第四批：体验补全（可与第三批并行）

10. 立绘支持（需素材组，最大视觉缺口）；气泡最大宽度上限；宅邸场景时段变体 + 厨房小池加厚；registry Step 5 ambient_remark；O-5 冷却确认。
11. 复核长会话 `history_len=30` vs L0 断言 `history<=8` 的口径差异，统一预期。

### 中期结构项（不阻塞版本，按批间隙推进）

12. gui.py 拆分第一步：先把领域解析器（`_streaming_segments`/`parse_twin_segments` 合一 + 搜索逻辑归一）抽出 UI 层成 `shared/` 纯函数，解锁单测；主窗口拆分另立专项。
13. 引入 ruff + pytest.ini（零配置→有底线），可选 GitHub Actions 跑 L0 全量（11s，无 API 成本）。
14. EVENT_POOL 外置迁移至 content/，完成 V14.4 注册表化路线的收尾。

---

## 七、总评

一个月内项目从「能跑的产品」进化为「带质量体系的产品」：审判循环让体验问题可发现可闭环，Forensic 让概率性崩溃可取证可复现，167 项零 API 测试 + 真机审判双轨是个人项目中罕见的完整度。当前最大的错位是**投入方向与风险现场的错位**——取证能力建在了不会分发出去的 CLI 侧，最需要它的 EXE 用户侧尚未接入；最大的隐患是**进度在本地堆积**——57 个提交与三件成果未落库未推送。第一批行动（半天）即可消除这两类暴露面，建议立即执行。
