# UI 重构说明（V9.3.0）

## 重构决策

原 Tkinter GUI 视觉上限低、自定义困难，决定将整个图形界面迁移到 **PySide6(Qt)**，以获得更好的视觉表现和长期扩展性。

## 主要改进

| 方面 | Tkinter 版 | PySide6(Qt) 版 |
|------|-----------|---------------|
| 布局系统 | 脆弱 | QHBoxLayout/QVBoxLayout，稳健 |
| 聊天气泡 | 文本堆砌 | 头像 + 圆角气泡，区分用户/AI |
| 视觉主题 | 原生风格 | QSS 统一主题，可扩展 |
| 动画/特效 | 无 | 易扩展淡入、打字机等效果 |
| 未来扩展 | 难 | 易做设置页、角色立绘、侧边栏 |

## 文件改动

- `gui.py`：完全重写为 PySide6 版
- `ReZeroTwin.spec`：增加 PySide6/shiboken6 hiddenimports
- 新增 `UI重构说明_2026-07-31.md`

## 保留的能力

- LLM 桥接模式（DeepSeek API）
- 本地模板模式（无 API Key 时自动回退）
- 记忆持久化（`data/memory.json`）
- 外部 `.env` 读取（不打包进 EXE）
- Enter 发送，Shift+Enter 换行

## 打包结果

- EXE 大小：约 **56.8 MB**（Qt 库增加导致，原 Tkinter 约 22 MB）
- 输出路径：`dist/ReZeroTwin.exe`
- 需同目录放置 `.env` 文件

## 待优化空间

1. 可加入蕾姆/拉姆主题配色（粉蓝/粉色系）
2. 可加入角色立绘或头像图片
3. 可加入设置面板（切换模式/篇章/清除记忆）
4. 可加入消息时间戳
5. 可加入打字机效果动画
