# v9.2.2 GUI 线程化

日期：2026-07-31
commit: `fix: GUI 调用 LLM 时不再阻塞主线程`

## 问题根因

`gui.py:on_send()` 在 Tkinter 主线程直接调用 `bot.chat()`（网络 I/O，DeepSeek 延迟 1~5 秒），
期间主循环堵死：窗口无法拖动、不刷新、被系统标记"未响应"。

## 修改内容（仅 gui.py）

- 新增 `import queue` / `import threading`
- `__init__` 新增 `_reply_queue` 与 `_waiting_reply`
- `on_send()`：入口加等待防护；LLM 分支置等待态（禁用输入/发送键）→ daemon 线程调用 → `root.after(100, _poll_reply)` 立即返回；local 分支保持同步不变
- 新增 `_fetch_llm_reply()`：worker 线程调 `bot.chat()`，异常兜底，结果入队（线程内不碰 Tkinter 控件）
- 新增 `_poll_reply()`：主线程轮询队列，收到后解除等待态并交 `_handle_reply()`
- 新增 `_set_waiting()`：禁用/恢复输入框与发送键，等待时状态栏提示"等待双子回复…"
- 新增 `_handle_reply()`：原有"解析 + 持久化 + 刷新状态栏"逻辑原样搬运，双模式共用

线程安全：等待期间输入被禁用，引擎只在 worker 写、主线程读（持久化/snapshot），无并发读写。

## 验证结果

1. py_compile ✓
2. 真实 GUI 自动化测试（真实 API 调用一次）：
   - 发送后立即断言：发送键/输入框 `disabled`、`_waiting_reply=True`（线程化生效）
   - 异步收到蕾姆+拉姆回复，按键自动恢复 `normal`，状态栏正确刷新
   - 测试前备份 `data/memory.json`，测试后已还原
3. `main.py --mode local` 回归 ✓
4. EXE 重新打包（含 v9.2.1 + v9.2.2），双击启动存活 ✓

## 行为变化

- LLM 模式：发送后界面立即可动，回复异步到达；等待期间无法重复发送（防护）
- local 模式：完全不变
