# v9.2.4 工程化底座（requirements + 冒烟测试）

日期：2026-07-31
commit: `chore: 新增 requirements.txt 与冒烟测试`

## 内容（纯新增，零风险）

- `requirements.txt`：openai / python-dotenv / pyinstaller（`>=` 下限，不锁死）
- `tests/smoke_test.py`：无框架冒烟测试，`python tests/smoke_test.py` 直接运行

## 覆盖项（7 项，不调 LLM、零 API 费用）

1. 引擎好感增减与风控关键词
2. 篇章切换（宅邸/帝国）
3. 记忆恢复与重逢奖励
4. snapshot() 无副作用
5. MemoryStore 默认值与读写（tempfile 临时目录隔离，不碰真实 data/）
6. PromptBuilder 输出关键约束字段
7. 本地模板模式一轮对话

## 验证结果

- `D:\python\python.exe tests/smoke_test.py` → 7/7 通过
- 托管 Python 3.13（无第三方依赖）交叉验证 → 7/7 通过
