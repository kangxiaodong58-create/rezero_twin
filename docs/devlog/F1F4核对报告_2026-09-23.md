# F1 / F4 只读核对报告 — 2026-09-23

> 依据：审计结论《SPEC-08 与 SPEC-09 审批 + M7 闭环状态判定》——批准两项只读核对（无需 SPEC）。
> 时点：2026-09-23 07:55（+0800）。核对对象：`master` `e4d11e9`（本地 ahead 7；`origin/master` 停在 `2b673fb`）。
> 性质：**只读**。除本报告 + INDEX 两处补强外零改动；核对逻辑以内存 python 执行，未落盘（无 Temp 残留）。

## F1 逐字验证：✅ 通过

**方法**：`git show cd32cba:AGENTS.md`（断流协议引入提交）vs `git show HEAD:AGENTS.md`，在「`# 附：rezero_twin 项目适配`」标记处切分，对**正文（标记之前）**做字节级比对。

| 对象 | 行数 | sha256[:16] |
|---|---|---|
| `cd32cba` 正文 | 73 | `d62b7c44e8c1fea1` |
| `HEAD` 正文 | 73 | `d62b7c44e8c1fea1` |

→ **正文自引入以来 byte-identical**：零改动、零追加。
→ 适配段 57 → 73 行（允许演进的仓库适配区：§0 授权规则 / §2 验证章 / §8 债务入口）。

**逐字断言（`grep -F`）7/7 命中**：身份句 · 红线① · 红线③ · L3 定义 · §0 授权原句 · 分支纪律原句 · 远端策略原句。

- **本次发现（已修）**：远端推送策略此前**只存了转述、未存原话** → 已把用户原句逐字补入 `docs/specs/INDEX.md` 的「远端推送策略」段。
- 正文内无仓库适配内容混入（扫描命中项均为正文自身文字，如「批准 SPEC-<ID>」口令）。

## F4 INDEX 回填核对：✅ 通过（附 1 处补强）

**方法**：INDEX 台账 9 行 ↔ `docs/specs/SPEC-*.md` 9 个文件 ↔ `git log master` / `git log origin/master` 三方交叉。

| SPEC | 状态 | 分支 | 合入提交 | master | origin |
|---|---|---|---|---|---|
| 01 断流协议落地 | ✅ | `feat/…protocol`（已删） | `cd32cba` | ✅ | ✅ |
| 02 两阶段提交与命令路由 | ✅ | `fix/…turn-commit`（已删） | `a211c61` | ✅ | ✅ |
| 03 SPEC 台账目录 | ✅ | `docs/…SPEC-registry`（已删） | `4a81c10` | ✅ | ✅ |
| 04 第 3 批 M6/M7 | ✅ | `fix/…a12-isolation`（已删） | `d6e0170` + 续批 `fbad672`/`b65a1dd`/`07b4f2f` | ✅ | 部分（续批本地待搬运） |
| 05 协议修订与旧报告废弃头 | ✅ | `docs/…protocol-revision`（已删） | `2b673fb` | ✅ | ✅ |
| 06 AGENTS 验证章刷新 | ✅ | `docs/…verification-refresh`（已删） | `89cbb18` | ✅ | 本地待搬运 |
| 07 GUI 组件树与状态归属审计 | ✅ | `audit/…gui-arch-audit`（已删） | `a010c11` | ✅ | 本地待搬运 |
| 08 conftest 自足化 | ✅ | `fix/…self-sufficient`（已删） | `0a807fb` | ✅ | 本地待搬运 |
| 09 AGENTS 债务入口刷新 | ✅ | `docs/…debt-refresh`（已删） | `e4d11e9` | ✅ | 本地待搬运 |

- 磁盘 9 个 SPEC 文件 ↔ INDEX 9 行：**一一对应，无缺、无多**。
- 状态列：**无 ⏳ 待批准行**（唯一 `⏳` 是「待批准清单」段标题，合法）。
- 「已推送」标注与 origin 事实**一致**（01–05 在 origin；06–09 明确标注「本地，随批量推送」）。
- **补强（本次）**：为 06–09 行补上真实 squash 哈希（原为「本批次 squash 提交」纯文字），使下一次 F4 核对可全机械通过。

## 结论

- **F1 ✅**：正文 byte-identical + 逐字断言 7/7；1 处原话补入（远端策略）。
- **F4 ✅**：9/9 行三方一致；1 处哈希补强。
- 本批**未跑** EXE / 真机：纯文档核对，无代码与行为变化。
