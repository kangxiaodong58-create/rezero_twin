# v9.5.1 仓库编排与推送

日期：2026-07-31
commit: `docs: 删除冗余英文文档并重构仓库文档编排`

## 内容

1. **删除冗余**：根目录 `English Readme`（376 行，与 README 内容重复）
2. **README 重构**：
   - 移除网页端追加的四段嵌入式英文附录（README 内嵌 code block 形式，展示效果差）
   - 新增顶部「文档导航」链接条、`项目结构`（真实结构）、版本路线表补 V9.2~V9.5 四行
   - 快速开始改为 `pip install -r requirements.txt`；指令清单补 `/llm` `/local`
   - 状态一览更新至 V9.5+（事件记忆、冒犯分层、拉姆通道）
3. **英文内容各归其位**：
   - `docs/README_en.md`：修正原文失效命令（`python rezero_bridge.py` → `python main.py --mode llm`）、占位维护者名 → 小东 & K
   - `docs/architecture.md`：Mermaid 架构图（GitHub 原生渲染），补 v9.3 事件记忆节点
   - `CONTRIBUTING.md`：根目录标准位置，更新为现行流程（冒烟测试 + devlog）
   - `docs/vision_module_structure.md`：原「建议模块结构」，明确标注为远期愿景（遵守不大规模重构原则）
4. **推送**：本地 8 个 commit（v9.2.6 评估文档 ~ v9.5.1）一次性 push 至 GitHub

## 说明

- 用户提供的第二个一次性 PAT 完成推送，用后删除
- 远程期间无分叉（fetch 确认），快进推送
