# 架构可视化审计（前端架构反向梳理法 · PySide6）施工记录 — 2026-09-22

> **SPEC**：SPEC-20260922-07（L0，零代码改动）· 分支 `audit/SPEC-20260922-07-gui-arch-audit`
> **交付物**：`docs/architecture/组件树与状态归属_V16_2026-09-22.md`（A 组件树 / B 状态矩阵 / C 痛点诊断 / D Roadmap）

## 1. 怎么扫的（方法论，可复用）

1. **机械骨架先出**（AST 扫描 `Temp/hermes-verify-struct-scan.py`）：全项目类/方法体量榜 + `gui.py` 组件骨架 + GUI 语义类名筛选 → 一眼看出 `TwinChatApp` 2100 行 / 62 方法 / `_setup_ui` 单方法 **413 行**。
2. **状态字段面**（AST + 正则 `Temp/hermes-verify-state-scan.py`）：`HardStateEngine` / `WorldState` 字段清单、`MemoryStore` 默认键与公开方法、`self.store.set/get` 调用点、`TwinChatApp` 的 `self.X =` 频次与首现行 → 状态矩阵原料。
3. **接线面**（grep 调用点）：刷新链（`_update_panels` 8 / `_update_status_bar` 6 / `_refresh_ambient` 1 / `_save_state` 5）、`_append_parsed_message` 30 处、`QTimer`13 处 / `QThread` 2 处 → 数据流三条主干。
4. **对照面**（只读既有台账 + Git）：A1–A12 / B–D 段逐条比对，判定「新债 or 补强」。
5. **只按区段读源码**：`__init__`(107) / `_setup_ui`(413) / `_handle_command` / `_update_panels` / `_save_state` / `eventFilter`——**不整读 3831 行**（SPEC 的锚点纪律）。

## 2. 机械判据（验收脚本 `Temp/hermes-verify-spec07-check.py`，跑完即删）

| 判据 | 结果 |
|---|---|
| Mermaid 自检（id 唯一 / 边两端已声明 / 无未识别行） | ✅ 1 块 · **54 id**（9 子图）· 25 边 |
| 真理源抽查（32 条 `file:line` 与源码逐条比对） | ✅ **32/32** |
| 覆盖度 | ✅ 组件树 **44 节点**（≥12）· 状态矩阵 **36 行**（≥30） |
| 诊断条目 | ✅ H4 / M6 / L4 = 14 条，全部带证据与建议级别 |
| 证据密度 | 全文 **158 处** `file:line` |

## 3. 三个坑（都值得记）

1. **`git clone --local` 只带已提交状态**（上一批踩过）：本轮把工作区文件 `cp` 进克隆体再验，CI 等价验证不再假阴性。
2. **AST 的 `self.X =` 扫描自带噪声**：把「控件引用」（`self.search_box`）与「业务状态」（`self._quote`）混在一起。必须人工按「是否被跨方法读写」筛一遍才成矩阵；纯自动归类会误报。
3. **既有台账行号已漂移 +5~+6 行**（V16.1–V16.3.3 的编辑所致）。本次**同时校正 8 条既有条目的行号**（C1/B3/B4/D1 等），并把「定位以函数名/类名为准」写进架构 Spec 抬头——否则下一次审计会拿旧行号读错代码。

## 4. 边界（没做什么）

- **未运行 GUI**（零代码改动 → 不打包 EXE、不真机）；全部结论为**静态推断 + 行号证据**，「未确认项」5 条单列在交付物 §7。
- **未改任何代码**；`git status --porcelain` 只列出本 SPEC 允许的文档文件。
- 判定为「既有台账已覆盖」的 8 条结论**没有重复发 ID**，只在架构 Spec 的「复核更新」表里补强（防重复登记，SPEC 明文要求）。
