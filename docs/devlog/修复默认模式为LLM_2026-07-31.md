: "llm"
}
```

## 验证

运行测试脚本：

```powershell
python test_mode.py
```

输出：

```
默认 mode: llm
保存 mode=local 后读取: local
保存 mode=llm 后读取: llm
```

## 重新打包 EXE

```powershell
pyinstaller ReZeroTwin.spec --clean
```

生成新的：

```
dist\ReZeroTwin.exe
```

## 运行效果

双击 EXE 后，状态栏应显示：

```
LLM 桥接模式 | 篇章：mansion_era | 蕾姆好感：15/100 | 拉姆：可疑 | 独立度：0.25
```

输入消息后，会调用 Deepseek API 生成蕾姆/拉姆的回复。

## 注意事项

- 如果之前运行过旧版本并生成了 `data/memory.json`，其中 `mode` 字段可能是 `local`
- 可以删除 `data/memory.json` 让程序重新默认使用 `llm`
- 或者在程序中发送 `/local` 或 `/llm` 指令切换（如已实现）
