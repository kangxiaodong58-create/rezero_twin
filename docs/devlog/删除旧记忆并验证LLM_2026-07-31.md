: "llm"，说明删除旧文件后程序能正确默认使用 LLM 桥接模式。

## 直接测试 API 调用

```powershell
python -c "from llm import ReZeroLLMBridge; from shared.state import StoryArc; bot = ReZeroLLMBridge(arc=StoryArc.MANSION_ERA); print(bot.chat('蕾姆'))"
```

输出：

```
【蕾姆】: "客、客人大人……您好。蕾姆是罗兹瓦尔宅邸的女仆，请问有什么需要帮忙的吗？"（低头行礼，声音轻柔带着些许紧张）
```

确认 Deepseek API 调用成功，角色回复正常。

## 重新打包 EXE

```powershell
pyinstaller ReZeroTwin.spec --clean
```

已成功生成新的 `dist\ReZeroTwin.exe`。

## 结论

- 删除旧的本地模式 `memory.json` 不会导致 LLM 桥接模式受损
- 删除后程序会重新生成默认 `mode: "llm"` 的记忆文件
- API 调用验证通过
- EXE 已重新打包，双击即可使用 LLM 模式
