# 修复 EXE 内嵌旧 API Key 问题

## 问题诊断

### 根因
`ReZeroTwin.spec` 中 `datas=[('.env', '.')]` 把 `.env` 打包进了 EXE 内部。  
PyInstaller onefile 模式下，打包进去的文件是**冻结时那一刻的快照**，后续修改外部 `.env` 不会影响 EXE 内部。  

因此用户更新 API Key 后，EXE 仍然使用旧的 `sk-a338...b6da5`，导致 `401 Authentication Fails`。

### 修复方案

**核心改动：`.env` 不再打包进 EXE，改为运行时从 EXE 同级目录读取。**

#### 1. `ReZeroTwin.spec`
```diff
- datas=[('.env', '.')],
+ datas=[],
```

#### 2. `shared/config.py`（已存在，无需修改）
`load_env()` 按以下顺序查找 `.env`：
1. PyInstaller 打包后：`EXE 同级目录/.env`
2. 源码运行：`项目根目录/.env`
3. 兜底：`当前工作目录/.env`

#### 3. `dist/.env`
新 Key 已复制到 `dist/.env`，与 `ReZeroTwin.exe` 同级。

### 验证结果

- `load_env()` 正确加载新 `.env`：`sk-a2b57365e...` ✅
- API 调用成功：蕾姆回复正常 ✅
- 旧记忆已清除，默认 `mode: "llm"` ✅

### 用户体验

以后更新 API Key 只需：
1. 编辑 `dist/.env` 文件
2. 重新双击 EXE

**无需重新打包 EXE。**
