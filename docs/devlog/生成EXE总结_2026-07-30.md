: 0.25,
  "recovery": 1.0,
  "arc": "mansion_era",
  "chat_history": [],
  "mode": "local"
}
```

### 4. GUI 聊天窗口

- 使用 Tkinter 实现
- 聊天区域 + 输入框 + 发送按钮
- 支持回车发送、Shift+回车换行
- 角色按颜色区分：蕾姆粉红、拉姆紫色
- 底部状态栏显示当前状态

### 5. 打包为单文件 EXE

使用 PyInstaller 打包：

```powershell
pyinstaller -F -w -n ReZeroTwin gui.py
```

生成文件：

```
dist\ReZeroTwin.exe
```

大小约 12MB，双击即可运行。

## 使用方法

### 双击运行

直接打开：

```
C:\Users\11985\.qclaw\workspace\rezero_twin\dist\ReZeroTwin.exe
```

### 命令行运行

```powershell
cd C:\Users\11985\.qclaw\workspace\rezero_twin
python gui.py
```

## 可用指令

| 指令 | 效果 |
|------|------|
| `/status` | 查看当前状态 |
| `/empire` | 切换帝国篇（失忆） |
| `/mansion` | 切换宅邸篇 |
| `/late` | 切换后期篇章 |
| `/recover 0.7` | 设置记忆恢复进度 |
| `/quit` / `退出` | 关闭程序 |

## 后续可优化方向

1. **添加头像和角色立绘**
2. **支持语音输入/输出**
3. **接入 LLM 桥接模式并处理 API 余额问题**
4. **支持多用户切换**
5. **添加聊天记录导出功能**
