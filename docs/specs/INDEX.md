# SPEC 台账（唯一索引）

> **SPEC 全文**：`docs/specs/SPEC-<ID>-<简述>.md`——一个 Spec 一个文件，直接打开即可审计，不需要翻聊天记录。
> **状态机**：`待批准 → 已批准 → 执行中 → 已完成 / 已回滚 / 已放弃`
> 对话里的批准口令一律是 `批准 [L0] SPEC-<ID>`；未收到口令前，agent 不得改动任何文件。

## SPEC 的核心价值（Agent 视角，2026-09-22 用户定调）

> SPEC 不是「留文档」，而是把 Agent 从**无状态聊天**变成**有状态施工**：外部记忆 + 执行契约 + 变更边界 + 验收标准 + 审计凭证。

| # | 价值 | 对 Agent 的具体作用 |
|---|---|---|
| 1 | **上下文锚点** | 会话中断 / 换模型 / 换 IDE / 隔几天再继续时，读 SPEC 即可恢复「目标 · 非目标 · 改了哪些文件 · 为什么这么改 · 怎么验收 · 怎么回滚」，不必重新讲背景，也不给 AI 自由补全漂移的空间 |
| 2 | **意图冻结** | 目标只做 A、非目标不做 B/C/D、范围只允许 @这些文件——防止「顺手优化」式扩需求 |
| 3 | **文件范围约束** | 明确的施工围栏：少扫全库、不碰无关模块、越界必须停下重新申请（独立开发没有多人审查，这是第一道安全网） |
| 4 | **任务分解** | 一个 SPEC 拆成可独立提交的小步（加接口 → 改实现 → 补测试 → 迁移调用方），继续开发可恢复、可暂停、可回滚 |
| 5 | **验收标准** | 写明测试方式与完成定义：做到什么程度停、跑哪些测试、什么算通过、什么算范围外 |
| 6 | **审批凭据** | 「批准 SPEC-xxx」是明确的状态转换：未批准只准讨论；已批准只准按 SPEC 施工；超范围必须重新申请 |
| 7 | **多 Agent / 多分支并行** | 一 SPEC 一分支，各自目标 / 范围 / 验收独立，减少冲突，便于接续与合并 |
| 8 | **回滚与风险控制** | 写明怎么退、哪些可独立撤销、哪些有数据 / 接口风险（L2/L3 没有回滚方案 = SPEC 不完整） |
| 9 | **知识沉淀** | SPEC 历史 = 变更原因数据库：热点文件为什么改过 5 次、哪些 SPEC 引入了复杂度——重构审计的依据 |
| 10 | **降低沟通与返工成本** | 审批一次按契约执行；你不在时不自由发挥；你回来时看 SPEC + 报告即可审查 |

| SPEC-ID | 级别 | 目标（一句话） | 状态 | 分支 | 合入提交 | 日期 |
|---|---|---|---|---|---|---|
| [SPEC-20260922-01](SPEC-20260922-01-断流协议落地.md) | L0 | 落地《断流协议》四角色提示词（AGENTS.md + docs/agents/*） | ✅ 已完成（用户「所有 Spec 全部批准」） | `docs/SPEC-20260922-01-duanliu-protocol`（已删） | `cd32cba` | 2026-09-22 |
| [SPEC-20260922-02](SPEC-20260922-02-两阶段提交与命令路由.md) | L2 | V16.2 两阶段提交 M3+M4/M5 + V16.2.1 命令路由归一 | ✅ 已完成（真机验证通过，已合入） | `feat/SPEC-20260922-02-turn-commit`（已删） | `a211c61` | 2026-09-22 |
| [SPEC-20260922-03](SPEC-20260922-03-SPEC台账目录.md) | L0 | 建立 SPEC 台账与审计目录 `docs/specs/` | ✅ 已完成 | `docs/SPEC-registry`（已删） | `4a81c10` | 2026-09-22 |
| [SPEC-20260922-04](SPEC-20260922-04-第3批M6M7与CI门禁.md) | L2 | 第 3 批：M6 文档真源收敛 + M7 CI 门禁 + `_active_txn` 残留；**续批 V16.3.2 CI 首跑修红 · V16.3.3 RCR 追加 A11/A12 数据隔离修复** | ✅ 已完成（用户批准，授权按建议路线实现）；RCR（A11+A12）已批准并落地；**M7 本地闭环留证 V16.3.6**（L3 远端首跑延至搬运日） | `fix/…-m6m7-cigate`（已删）· `fix/…-ci-first-run`（已删）· `fix/…-a11-a12-isolation`（已删） | `d6e0170` + 续批 `fbad672`/`b65a1dd`/`07b4f2f`（本地，随批量推送） | 2026-09-22 |
| [SPEC-20260922-05](SPEC-20260922-05-协议修订与旧报告废弃头.md) | L0 | 授权规则入档（AGENTS.md §0）+ V10.4 旧报告废弃头 | ✅ 已完成（用户批准，已推送） | `docs/SPEC-20260922-05-protocol-revision`（已删） | `2b673fb` | 2026-09-22 |
| [SPEC-20260922-06](SPEC-20260922-06-AGENTS验证章刷新.md) | L0 | AGENTS.md 验证章刷新：基线 294→295 / 隔离语义 / CI 事实 | ✅ 已完成（用户批准） | `docs/SPEC-20260922-06-agents-verification-refresh`（已删） | `89cbb18`（本地，随批量推送） | 2026-09-22 |
| [SPEC-20260922-07](SPEC-20260922-07-GUI组件树与状态归属审计.md) | L0（零代码，证据深度按 L2 交付） | GUI 组件树 + 状态真理源矩阵 + 架构痛点诊断 + 重构 Roadmap（前端架构反向梳理法 · PySide6 版） | ✅ 已完成（用户批准） | `audit/SPEC-20260922-07-gui-arch-audit`（已删） | `a010c11`（本地，随批量推送） | 2026-09-22 |
| [SPEC-20260922-08](SPEC-20260922-08-conftest自足化.md) | L1 | 测试套件自足化：conftest 注入 dummy API key（闭合 A19 → M7 本地闭环判据①③） | ✅ 已完成（审计批准；裸环境 + CI 等价双 295 passed 留证） | `fix/SPEC-20260922-08-conftest-self-sufficient`（已删） | `0a807fb`（本地，随批量推送） | 2026-09-22 |
| [SPEC-20260922-09](SPEC-20260922-09-AGENTS债务入口刷新.md) | L0 | AGENTS.md §8 债务入口刷新（A1–A19 / M1–M7 全落地）+ §2 门禁闭环口径指针 | ✅ 已完成（审计批准；含 F7-1 同批修） | `docs/SPEC-20260922-09-agents-debt-refresh`（已删） | `e4d11e9`（本地，随批量推送） | 2026-09-22 |
| [SPEC-20260922-10](SPEC-20260922-10-CI首跑修红与可观测性.md) | L1 | CI 首跑修红：失败取证 annotations 化（免登录可远程定位）+ Qt 夹具可移植化（session 级 `qapp`） | ✅ **已闭环**（本地 295 passed；远端三跑 `#35800786078` **success** → L3 闭环） | `fix/SPEC-20260922-10-ci-red-portable-fixture`（已删） | 见 `docs/devlog/L3远端首跑留证_2026-09-23.md` | 2026-09-23 |
| [SPEC-20260922-12](SPEC-20260922-12-第4批B0B2零观感梯队.md) | L0+L1 | 第 4 批：GUI 首梯队 **B0–B2**（零观感）+ 债务 **A13/A15/A16/A17** 收口 | ✅ **已完成** | `fc46452` | 4 条（`281a0fa`/`5025a98`/`dd17169`/`2f7f79a`，分支已删） | 2026-09-23 |
| [SPEC-20260922-11](SPEC-20260922-11-L3闭环与AGENTS刷新.md) | L0 | L3 闭环留证回填 + AGENTS §8 门禁状态刷新（L2+L3 双闭环终态） | ✅ 已完成 | `docs/SPEC-20260922-11-L3-closure`（已删） | 本批次 squash 提交（本地→随推送） | 2026-09-23 |

## ⏳ 待批准清单（等你一句话才动文件）

| SPEC | 级别 | 一句话 | 依据 |
|---|---|---|---|
| SPEC | 级别 | 内容 | 依赖 |
|---|---|---|---|

口令：**批准 SPEC-20260922-12** 需要时我按 `docs/agents/SPEC_AGENT.md` 的格式起草新 SPEC 落到本目录，并在此登记状态。

候选立项（来自债务台账，需先出 SPEC 才能动手）：

| 候选 | 依据 | 级别 |
|---|---|---|
| A19 测试套件自足化（conftest dummy key）→ **已立 SPEC-08** | 台账 A19 + M7 留证 §2 | L1 |
| GUI 重构 **B0–B2**（删破坏性遗留脚本 `test_mode.py` · 顶栏死 UI 标注 · 删 `self.mem` 陈旧读 · 世界行统一格式化 · 删 `/toggle` 死命令与 `mode` 字段） | [组件树与状态归属审计](architecture/组件树与状态归属_V16_2026-09-22.md) §5 + 台账 A13/A15/A16/A17 | L0/L1（可一批做完） |
| GUI 重构 **B3**（僵尸面板双写收口） | 台账 D3 + 审计 §3-H2 | L1/L2（观感类，需真机） |
| GUI 重构 **B4**（第二 WorldState 收口） | 台账 B6 + 审计 §3-H3 | L2 |
| GUI 重构 **B5**（导航数据驱动化 + 删重复坞） | 台账 D1 + 审计 §3-M1 | L2（观感类，需真机） |
| GUI 重构 **B6**（样式收口 `ui/theme.py`） | 台账 A14 + 审计 §3-M2 | L2（视觉零变化 + 截图基线） |
| GUI 重构 **B7**（搜索合并 `SearchService`） | 台账 B4 + 审计 §3-M6 | L2 |
| GUI 重构 **B8**（拆 `ui/` 包，六步迁移） | 台账 C1 + 审计 §4.3 | **L3** |
| A10 并发收口（非原子整快照提交 / 撕裂读 / `_anniv_cache`） | 架构 Spec §4.6 + 台账 A10 | L2/L3 |
| B1–B5 结构性债务（事件×天气 4 套判定 / `SCENE_CN` 3 份 / 双子解析器 2 份 / `world_state.py` 兼容层 / `EVENT_POOL` 硬编码 / 左导航 4 空壳） | 架构 Spec §6.B/§6.D | L2 各自独立 |

> 已闭环：A11/A12（V16.3.3 修复并验收）；A1–A9 见架构 Spec §7 台账（未标 ✅ 的仍成立）。

## 分支纪律（2026-09-22 定稿）

- **一个 Spec 一个分支**（不是每个 commit 一个分支）
- 同时活跃分支 **≤ 2 个，最好 1 个**
- 分支寿命：**1 天内最好 / 3 天可接受 / 超过 1 周必须拆或放弃**
- 合并方式：**squash merge**——master 历史是「一条 Spec 一条提交」，干净可回滚
- 合并后**立刻删本地 + 远程分支**；代码平台开启「合并后自动删除分支」
- 每周 5 分钟清理：`git branch --merged master`（本地）/ 平台已合并分支列表（远程）
- 分支命名：`feat|fix|refactor|audit|docs/SPEC-<ID>-简述`
- 提交信息格式：`type(scope): 描述 [SPEC-<ID>]`

## 远端推送策略（2026-09-22 用户决定）

> 用户原话（逐字）：**「如果github侧不稳定，可以暂时先放弃github每次提交等，等到时候真正完成的时候，进行全量整合批量搬运即可」**

- **GitHub 侧不稳定时不逐次 push**：本地照常按上节纪律 squash 合入 `master`，**阶段性完成（或网络稳定）时一次性批量推送** `git push origin master`。
- 因此 **CI 首跑 / 复跑的验证时点顺延**到那次批量推送之后；在此之前 CI 的等价验证用「**干净克隆复现法**」（`git clone --local` 到 scratch + 项目 venv 跑同款命令）在本地完成。
- 本地 `master` 与 `origin/master` 的差值就是待搬运量，用 `git status -sb` 一眼可见。
- **首次批量搬运（2026-09-23，已完成）**：`2b673fb..290eb9f` → 首跑 `#35800087797` 红 → 修（取证步 + Qt 夹具）→ 复跑 `#35800505406` 红（annotations 定位 A20）→ 修 → **三跑 `#35800786078` success**；全过程留证 `docs/devlog/L3远端首跑留证_2026-09-23.md`。随后 留证提交 `…e863fdd` 再推一次 → `origin/master` 与本地同步（**待搬运量 0**）。

## GUI 重构排期（F7-3 决策，2026-09-23 用户定案）

> 用户口令：「**按A**」= 采用审计建议方案 A——**按梯队推进 + 与 A13–A18 债务合并成批次**。

| 批次 | 内容 | 真机确认 | 状态 |
|---|---|---|---|
| **第 4 批** | **B0–B2**（零观感）+ 债务 A13/A15/A16/A17；A18 只做计时探针 | 否 | ✅ **已执行 V16.4.0**（`fc46452`）——A18 实测 94× 提速，改法另批；`/toggle` 保留（绑「设置」入口） |
| 第 5 批 | **B3**（僵尸面板）· **B5**（导航数据驱动化） | **是** | 待排 |
| 第 6 批 | **B6–B8**（样式收口 / 搜索合并 / `ui/` 拆包 L3） | 按需 | 待排 |
| 并行线 | 功能/内容线（不受阻）· A10 并发收口（独立项） | 按需 | 待排 |

依据：`docs/architecture/组件树与状态归属_V16_2026-09-22.md` §5 Roadmap；债务台账 A13–A18（A19/A20 已闭环）。

## M7 闭环判据与豁免条款（2026-09-22 审计修订）

> 来源：审计结论《补充约束确认：GitHub 侧不稳定 → M7 判定标准需修订》。**本节是可审计的豁免条款**——后续审计直接引用，不再重新讨论。

### 三层拆分（配置存在 ≠ 门禁生效）

| 层 | 内容 | 受 GitHub 稳定性影响 |
|---|---|---|
| **L1 门禁配置** | `.github/workflows/ci.yml` 存在 | 否 |
| **L2 门禁本地等价** | `scripts/verify_isolation.sh` + `pytest` + `smoke_test.py` 本地**真跑并留证** | **否——M7 事实闭环落在这里** |
| **L3 门禁远端生效** | GitHub Actions 真实跑绿 | 是——**可延后，不可取消** |

### L2 闭环判据（四条，机械核验）

1. `bash scripts/verify_isolation.sh <venv>/python.exe -m pytest tests/ -q` → **295 passed** 且 `data/` 逐字节未变；
2. `tests/smoke_test.py` → **31/31**；
3. 两项在**干净 checkout**（无 `.env`、无 `data/`、无宿主 `PYTHONPATH`）下跑通；
4. 以上三条**有留证**（devlog 或 CHANGELOG 记录命令与结果）。

### 豁免边界（不可豁免项）

- 「GitHub 不稳定」**豁免的是「每次提交都推」，不豁免「门禁真实生效」**；
- 不豁免：① 本地门禁必须真跑（不依赖 GitHub）；② 结构守卫（AST 扫同类重复方法）本地生效；③ 数据隔离快照 diff 本地执行；④ **最终批量搬运日必须补一次全量 CI 并留证**。

### 当前状态（2026-09-22）

- 留证据：`docs/devlog/M7本地闭环留证_2026-09-22.md`（干净克隆 `a010c11`，逐条命令与原始输出）。
- 判据 ②③④ 成立；判据 ① 在 **CI 等价环境**（`DEEPSEEK_API_KEY=test-key-not-used`，同 `ci.yml:25`）成立（`295 passed` + 隔离通过），**裸环境不成立**（`9 failed, 286 passed`，全部 `test_ui_offscreen.py` 缺 key）→ 缺口 = **A19**，修法见 `SPEC-20260922-08`。
- **M7 本地闭环 = ✅ 已成立（L2，2026-09-22）**：SPEC-08 执行后**裸环境**（`env -u PYTHONPATH -u DEEPSEEK_API_KEY`）与 **CI 等价环境**均 `295 passed` + 隔离校验通过；留证见 devlog §4。
- L3 远端门禁：**已触发、有留证、尚未转绿**——首次搬运 `2b673fb..290eb9f` → 首跑 `#35800087797` 红（步骤 7；日志 403、artifacts=0 → 取证受阻）→ 加取证步 + Qt 夹具可移植化（`SPEC-20260922-10`）→ 二跑 `f181fb5` `#35800505406` 仍红，但**取证步把真因写进了 annotations**：`tools/screenshot_baseline.py` 的 `compare()` 与 `capture()` 渲染尺寸不对称（新债 **A20**，Windows 靠 Qt 隐式重排侥幸通过）→ 已修并本地验证（五面 diff 0.00%）→ 三跑待回填。****第 3 跑 `#35800786078`（`e863fdd`）success（全步骤 ✅，40s）→ L3 闭环、M7 完整闭环（L2+L3）**；冻结的解除条件（L3）已满足，是否正式解冻按用户/审计口径宣布。

## 范围外变更请求（RCR）模板

执行中一旦发现需要改 Spec 未列出的文件，**立即停止**并按此格式输出：

```
范围外变更请求
- 触发：正在执行 SPEC-<ID> 的哪一步
- 需要动的文件（Spec 外）：@路径
- 为什么必须动：<根因，不是"顺手优化">
- 不动的后果：
- 最小改法：
- 是否可延后为独立 SPEC：是/否
等待批准：是
```

## 与其它文档的关系

| 文档 | 角色 |
|---|---|
| `docs/specs/INDEX.md`（本文件） | SPEC 台账与流程纪律（唯一索引） |
| `AGENTS.md`（已落地 SPEC-01） | 项目级总提示词：三条硬红线 + 改动分级 + 模式切换 + 报告格式；§0 授权规则（批准即授权实现到合并） |
| `docs/工程惯例.md` | 既有工程惯例真源（技术约定细节） |
| `docs/architecture/逆向架构Spec_V16_2026-09-22.md` | 技术架构现状 + 债务台账 A1–A12（历史债务的登记处） |
| `docs/devlog/` | 每个版本/批次的施工记录（做了什么、怎么验的、踩了什么坑） |
