```
SPEC-ID：SPEC-20260922-05
级别：L0（纯文档）
状态：✅ 已完成（用户批准）
分支：docs/SPEC-20260922-05-protocol-revision（已删除）
```

# 协议修订：授权规则入档 + 旧报告废弃头

## 目标

1. 把用户 2026-09-22 授予的授权规则**逐字**写进 `AGENTS.md`——「以后Spec一旦我审批批准，你就有权限按我的批准报告或者按照建议路线进行实现」；
2. 给 V10 时代旧报告 `技术研判报告_V10.4.0.md` 补废弃头（V16.3 施工记录 §5 登记的遗留项）；
3. 回填台账与 CHANGELOG。

## 非目标

- 不动任何代码、不改 `data/`、不处理 A10 并发收口与 B1–B5 结构性债务（各自另立 SPEC）

## 需要读取/修改的文件

- @AGENTS.md（新增「## 0. 授权规则（2026-09-22 用户授予，最高优先级）」，位于项目适配段首位）
- @技术研判报告_V10.4.0.md（顶部加废弃头）
- @docs/specs/INDEX.md（新增本 SPEC 行）
- @CHANGELOG.md（V16.3.1 条目）

## 新增文件

- `docs/specs/SPEC-20260922-05-协议修订与旧报告废弃头.md`（本文件）

## 接口/数据是否变化

无。

## 测试方式

无代码测试（纯文档）。验证 = ① `grep` 断言 AGENTS.md 含授权条款且为用户原句；② 三份旧报告（`docs/architecture.md`、`技术研判报告_V14.8_*.md`、`技术研判报告_V10.4.0.md`）首屏均为废弃头；③ `pytest tests/ -q` 仍 295 passed（无代码改动，跑一遍确认无意外）。

## 回滚方式

`git revert <该批次 squash 提交>`（纯文档，零运行时影响）。

## 风险

低。唯一风险是授权条款措辞偏离本意 → 已采用**逐字引用用户原句 + 一句操作含义**的写法，不含引申。

## 等待批准：是。请回复：批准 L0 SPEC-20260922-05

## 执行记录（2026-09-22）

- 三项全部完成；`pytest tests/ -q` → 295 passed 无回归。
- 附带：`AGENTS.md` 项目适配段现有 10 节（0 授权规则 → 1 分支纪律 → … → 9 其它角色提示词）。

## 修订

- v1（2026-09-22）：草案（随批准落地）。
