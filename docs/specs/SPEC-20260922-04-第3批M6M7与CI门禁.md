```
SPEC-ID：SPEC-20260922-04
级别：L2（含新增文件：CI 门禁配置；含一处代码小修）
状态：✅ 已完成（用户批准；授权按批准报告/建议路线实现）
分支：fix/SPEC-20260922-04-m6m7-cigate（已删除）
```

# 第 3 批整改：M6 文档真源收敛 + M7 CI 门禁 + 残留清理

## 目标

1. **M6 文档真源收敛**：给过期报告加废弃头；架构 Spec 补「并发/线程边界」维度；把已知债登记完整。
2. **M7 CI 门禁**：让「测试 + 结构守卫 + 数据隔离」在提交前自动拦，不再依赖人工记得跑。
3. 清掉 SPEC-02 登记的残留：`_active_txn` 在取消/流中断分支未清引用。

## 非目标

- 不动结构性债务（事件×天气兼容 4 套、`SCENE_CN` 3 份、双子解析器 2 份、`shared/world_state.py` 兼容层、`EVENT_POOL` 硬编码、左导航 4 个空壳）——属后续独立 SPEC
- 不引入新依赖（CI 只用仓库自带 venv + 现成 pytest）
- 不改状态机提交语义、不改存档格式

## 需要读取/修改的文件

- @docs/architecture/逆向架构Spec_V16_2026-09-22.md（补「并发/线程边界」章节 + 债务登记）
- @docs/architecture.md（旧架构文档，加废弃头指向新 Spec）
- @docs/技术研判报告_V14.8_2026-08-29.md（同上，加废弃头）
- @llm/bridge.py（残留：取消/流中断分支 `self._active_txn = None`）
- @docs/specs/INDEX.md（本 SPEC 状态回填）

## 新增文件

- `.github/workflows/ci.yml`：push/PR 触发，跑 ① `pytest tests/ -q` ② `smoke_test.py` ③ 数据隔离快照 diff（跑测试前后 `data/` 必须逐字节不变）④ AST 重复方法名结构守卫（已含在 pytest 内，单独列出以便 CI 日志可读）
- `scripts/verify_isolation.sh`：CI 与本地共用的隔离校验脚本（`stat` 快照 + `diff`）

## 接口/数据是否变化

无接口变化、无数据格式变化。唯一代码改动是 `llm/bridge.py` 两处赋值（清理悬空事务句柄引用）。

## 测试方式

- `env -u PYTHONPATH venv/Scripts/python.exe -m pytest tests/ -q`（期望 294 passed，新增 CI 脚本用例后 ≥294）
- `env -u PYTHONPATH venv/Scripts/python.exe tests/smoke_test.py`（31/31）
- 本地模拟 CI：`bash scripts/verify_isolation.sh`（跑完 `data/` 逐字节不变）
- 新增用例：`tests/test_turn_commit.py` 增 1 条——取消/流中断后 `bot._active_txn is None`

## 回滚方式

`git revert <该批次 squash 提交>`。CI 文件删除即失效；文档改动无运行时影响；`bridge.py` 两行赋值可单独 revert。

## 风险

- **中**。CI 首次运行可能暴露既有测试在干净环境（无 `.env`、无 `data/`、无宿主 PYTHONPATH）下的脆弱点——这是**预期收益**，但要预留一轮「CI 首跑修红」的预算。
- CI 环境为 Linux（GitHub Actions），本仓为 Windows 开发：路径分隔符、`REZERO_GUI_LOG`、`QT_QPA_PLATFORM=offscreen` 需在 workflow 里显式设置；PySide6 在 Linux runner 上需装系统库（`libgl1` 等）——若装不上，退化为「CI 只跑非 GUI 测试子集 + 结构守卫 + 隔离校验」，GUI 测试保留本地。
- 文档加废弃头是纯增量，无风险。

## 等待批准：是。请回复：批准 SPEC-20260922-04

## 执行记录（2026-09-22）

- **结果**：全部完成。`pytest tests/ -q` → **295 passed**（294 → +1）；`bash scripts/verify_isolation.sh …` → ✅ 隔离通过（反向探针能抓）；冒烟 **31/31**。
- **范围修正（诚实登记）**：
  - `技术研判报告_V14.8_2026-08-29.md` SPEC 写作 `docs/` 路径，**实际在仓库根** → 按实际路径执行；
  - `tests/test_turn_commit.py` 未列入「需要修改的文件」，但本 SPEC「测试方式」明确要求补 1 条用例 → 已补（`test_active_txn_cleared_on_cancel_and_stream_error`）；
  - `技术研判报告_V10.4.0.md` 未列入本 SPEC，**未加废弃头**（建议后续 L0 批次处理）。
- **CI 首跑**：未在本地验证（无 Linux runner）。`ci.yml` 需要一次真实 push 才能跑起来；若 Qt 系统库/offscreen 在 runner 上不可用，按本 SPEC 风险段退化为「非 GUI 子集 + 结构守卫 + 隔离校验」。
- **附带产出**：债务台账新增 **A10**（跨线程并发写 / 撕裂读 / 陈旧整快照回写），架构 Spec 新增 §4.6 线程模型与并发边界。

## 修订

- v1（2026-09-22）：草案。
- v2（2026-09-22）：用户批准（授权按建议路线实现）；执行完成，状态回填。
