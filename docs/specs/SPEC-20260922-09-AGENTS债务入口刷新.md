```
SPEC-ID：SPEC-20260922-09
级别：L0（纯文档）
状态：✅ 已完成（审计批准 2026-09-23；依赖 SPEC-08 已满足）
分支：docs/SPEC-20260922-09-agents-debt-refresh（已删除，squash 合入）
```

# AGENTS.md 债务入口刷新（§8 已过期）+ 门禁闭环口径一行

## 目标

`AGENTS.md` 的「项目适配」段里两处已过期，对齐当前事实（V16.3.6）：

1. **§8 债务入口**：「债务台账 **A1–A9**（已修者标 ✅）… 架构审批强制整改项 M1–M7 中，M1–M5 已落地，M6/M7 待第 3 批」→ 改为：台账 **A1–A19**（含 GUI 审计 A′ 段），**M1–M7 全部已落地**（M7 = 本地等价门禁留证闭环，远端首跑按豁免条款延至搬运日）。
2. **§2 表格下方**增一行「门禁闭环口径」指针：指向 `docs/specs/INDEX.md` 的「M7 闭环判据与豁免条款」段（避免下次审计重新讨论「配置存在 vs 门禁真跑」）。

## 非目标

- 不改「断流协议」正文（用户原文逐字保留）；
- 不改 §0 授权规则、§1 分支纪律、§3–§7 的既有条款（除 §2 增一行指针、§8 刷新事实）；
- 不动任何代码 / 测试 / CI。

## 需要读取/修改的文件

- @AGENTS.md（§2 增一行 + §8 整段刷新）
- @docs/specs/INDEX.md（状态回填）
- @CHANGELOG.md（V16.3.8）

## 新增文件

- `docs/specs/SPEC-20260922-09-AGENTS债务入口刷新.md`（本文件）

## 接口/数据是否变化

无。

## 测试方式（机械判据）

1. `grep` 断言：AGENTS.md 含 `A1–A19`、含 `M1–M7`（不再出现「M6/M7 待第 3 批」）、含「M7 闭环判据与豁免条款」；
2. `pytest tests/ -q` 仍 `295 passed`（确认无代码改动）；
3. 用户原话段落**逐字未变**（对比 `断流协议` 正文 + §0 授权原句，用 `grep -F` 断言）。

## 回滚方式

`git revert <本批次 squash>`（纯文档）。

## 风险

低。等 SPEC-08 执行完再刷 §8（这样 §8 写的就是「M7 已闭环」的终态）——**依赖：SPEC-08 先执行**。

## 等待批准：已批准（审计结论《SPEC-08 与 SPEC-09 审批 + M7 闭环状态判定》，2026-09-23；条件 = SPEC-08 先执行并留证 → **已满足**）

## 执行记录（2026-09-22 深夜）

- **§8 刷新**：`A1–A9 / M6,M7 待第 3 批` → **`A1–A19`（含 GUI 审计 A′ 段）+ M1–M7 全部已落地**（M7 = 本地等价门禁留证闭环；L3 延至搬运日补、不可取消）；同时补界面层审计文档入口。
- **§2 新增「门禁闭环口径」指针**：指 `docs/specs/INDEX.md` 的「M7 闭环判据与豁免条款」段（回应 F2 的可机械化部分）。
- **同批附带（F7-1）**：删除 `docs/specs/SPEC-20260922-07-…md` 末尾**重复的**「## 修订」段（此前执行回填时多留了一份 v1）——该文件属 `docs/specs/**` 免二次批准范围。
- **未改**：断流协议正文、§0 授权规则、§1 分支纪律、§3–§7 既有条款（逐字保护，`grep -F` 断言通过）。
- **未跑项及原因**：打包 EXE / 真机——纯文档，无观感或行为变化。

## 修订

- v1（2026-09-22）：草案。
- v2（2026-09-22 深夜）：执行完成回填（§8 终态 + §2 指针 + F7-1 同批修）。
