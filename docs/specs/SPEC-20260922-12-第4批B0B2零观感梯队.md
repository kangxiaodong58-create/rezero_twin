```
SPEC-ID：SPEC-20260922-12
级别：L0（B0）+ L1（B1、B2 三项）——整体按 L1 纪律执行，逐项独立提交、可单项回滚
状态：⏳ 待批准
```

# 第 4 批：GUI 重构首梯队 B0–B2（零观感风险）+ 债务 A13/A15/A16/A17 收口

## 背景与依据

- **F7-3 决策（用户 2026-09-23 定案）**：采用**方案 A**——按梯队推进（B0–B2 零观感 → B3/B5 需真机 → B6–B8 设计级），并与 A13–A18 债务**合并成批次**。
- Roadmap 原文：`docs/architecture/组件树与状态归属_V16_2026-09-22.md` §5（行 407–420）。
- 债务细节：`逆向架构Spec_V16_2026-09-22.md` 的 A13 / A15 / A16 / A17 条目 + A′ 段补强表（A3、D3 实例）。

## 目标

按 Roadmap 首梯队执行 **B0 + B1 + B2**（三项判据均为「无需真机」），顺带清掉对应台账条目。

### B0（L0）— 破坏性遗留 + 死 UI 标注

| 项 | 内容 | 判据 |
|---|---|---|
| **A16** | 删除工作区遗留破坏性脚本 `test_mode.py`（未跟踪文件；`test_mode.py:11-12` 会 `os.remove(data/memory.json)`，且 `:6-7` 用 `PROJECT_ROOT` 而非 `get_data_dir()`） | `git status` 无该文件；内容先贴入本 SPEC 附录（可回滚）；CHANGELOG/台账登记「已删」 |
| **A17** | 顶栏两个角色 tab 与「双子模式」标签（`gui.py:1898-1918`，全为局部变量、无 `self.` 引用、无交互）→ **只加显式注释标注为装饰性死 UI** | 零观感变化（纯注释）；「接成真实入口」方案写入台账，**列为需真机项后续处理** |

### B1（L1）— `self.mem` 陈旧快照收口

- 事实（A13）：`gui.py:1661 self.mem = self.store.load()` 是加载期快照；读点仅 2 处（`gui.py:1849` 取 `arc`、`1857` 取 `engine`），写入仍走 `_save_state → store.save`（`gui.py:3368`）与 `store.set`（1671/1839）。
- 改法：两处读点改走运行期真源（`bot.engine` 对应字段）；随后**删除 `self.mem` 快照**。
- 判据：`grep -n "self\.mem" gui.py` = **0 处**；全量 295 passed；`test_state_persistence.py` 覆盖重启恢复一致。

### B2（L1 ×3）— 三处「双实现/死状态/硬编码」收口

| 子项 | 内容 | 判据 |
|---|---|---|
| **A15** 世界行统一格式化 | 抽 `format_world_line()` 单一实现；`_refresh_ambient`（`gui.py:3133`）与 `_update_status_bar`（`gui.py:3168`）改为调用它。**截断长度作为参数保留现值**（3132 用 14 字 / 3158 用 16 字）→ **文案零变化**；「两处是否该统一长度」登记为待真机决策 | 截图基线逐张通过；两处渲染文本与改前逐字相同（探针断言） |
| **A3 实例** 死状态 | 删除 `/toggle` 死命令与 `mode` 字段（A3 的 M4 实例） | 先只读侦察确认无引用；`test_command_router.py` 全绿；全量 295 passed |
| **D3 实例** 硬编码映射 | 拉姆表情映射硬编码 → 移入 `design_tokens`（token 化） | `test_screenshot_baseline.py` 逐张通过（视觉零变化） |

## 非目标

- **A14（样式收口到 `ui/theme.py`）**：属 **B6**（L2、需真机、71 处 `setStyleSheet(`），**本批不做**；
- **A18（`_log` 缓冲写）**：本批只做**只读 A/B 计时探针**（产出数据写 devlog），改与不改另批决定；
- B3/B5（需真机）、B6–B8、A10 并发收口、`gui.py` 拆包（B8）、`main.py`/第二 WorldState（B4）——均不在本批；
- 不新增功能、不改文案、不改观感、不动存档格式。

## 需要读取/修改的文件

- @gui.py（B1 两处读点 + `self.mem` 删除；B2 的世界行/命令路由/表情映射点）
- @design_tokens.py（D3 实例：表情映射 token 化）
- @test_mode.py（B0：**未跟踪**文件的本地删除）
- @CHANGELOG.md、@docs/specs/INDEX.md、@docs/devlog/**（四件套回填）

## 新增文件

- `docs/specs/SPEC-20260922-12-第4批B0B2零观感梯队.md`（本文件）
- `docs/devlog/B0B2零观感梯队_2026-09-23.md`（施工记录，含 A18 计时数据）
- `Temp/hermes-verify-*.py`（一次性探针，**跑完即删**）

## 接口/数据是否变化

无接口变化；无数据/存档格式变化；无文案与观感变化（B2 三子项均以「逐字相同 / 像素基线通过」为硬判据）。

## 测试方式（机械判据，全部本地零 API）

1. 每完成一项：隔离脚本包裹 `pytest tests/ -q` → **295 passed** 且 `data/` 逐字节未变；
2. B1：`grep -c "self\.mem" gui.py` = 0；`pytest tests/test_state_persistence.py -q` 全绿；
3. B2-A15：探针断言两处世界行文本与改前逐字一致；
4. B2-A3：`pytest tests/test_command_router.py -q` 全绿 + `/toggle` 无引用（grep 0 处）；
5. B2-D3：`pytest tests/test_screenshot_baseline.py -q` 全绿（截图基线逐张通过）；
6. 收尾：`tests/smoke_test.py` → **31/31**；`git status` 仅列本 SPEC 允许文件；
7. **真机确认**：B0–B2 判据均为「否」——若任何一项出现文案/像素差异，**立即停下报告**（视为超范围）。

## 回滚方式

逐项独立提交（`type(scope): … [SPEC-20260922-12]`）→ 可单项 `git revert`；B0 的 `test_mode.py` 删除不进库（未跟踪），回滚 = 从本 SPEC 附录还原内容。

## 风险

- B1：若存在**非字面**的 `self.mem` 使用（如同名局部变量、`getattr`）→ 用全量 grep + 全量测试双重兜底；
- B2-A15：抽函数时若语义漂移（如空事件、None 分支）→ 逐字对照探针兜底；
- B2-D3：token 化若改动颜色取值 → 截图基线兜底（失败即回滚该项）；
- A17 只加注释，若注释被判为「需要真机确认」的改动 → 以零行为变化说明豁免（注释不参与运行）。

## 附：`test_mode.py` 全文（删除前留档，供回滚）

> 实测全文（25 行，2026-09-23 读盘留存）。`git ls-files test_mode.py` 为空 → **未跟踪**，删除不进库；本附录即回滚源。

```python
"""测试 MemoryStore 是否能正确保存和读取 mode。"""

import os
from shared.memory_store import MemoryStore

PROJECT_ROOT = os.path.dirname(os.path.abspath(__file__))
store = MemoryStore(PROJECT_ROOT)

# 测试 1：默认 mode
os.makedirs(os.path.join(PROJECT_ROOT, "data"), exist_ok=True)
if os.path.exists(os.path.join(PROJECT_ROOT, "data", "memory.json")):
    os.remove(os.path.join(PROJECT_ROOT, "data", "memory.json"))

mem = store.load()
print(f"默认 mode: {mem.get('mode', 'NOT SET')}")

# 测试 2：保存 mode=local 后读取
store.set("mode", "local")
mem = store.load()
print(f"保存 mode=local 后读取: {mem.get('mode', 'NOT SET')}")

# 测试 3：保存 mode=llm 后读取
store.set("mode", "llm")
mem = store.load()
print(f"保存 mode=llm 后读取: {mem.get('mode', 'NOT SET')}")
```

**删除必要性（实测确认）**：`:11-12` 会 `os.remove(<PROJECT_ROOT>/data/memory.json)`——**直接删真实存档**；`:6-7` 用 `PROJECT_ROOT` 而非 `get_data_dir()`（违反 V16.1 路径纪律）；且它测的 `mode` 字段正是 B2 要删的 A3 死状态（互为佐证）。

## 等待批准：是。请回复：**批准 SPEC-20260922-12**
