```
SPEC-ID：SPEC-20260922-01
级别：L0（纯新增文档/配置；不动代码、数据、依赖、权限）
状态：⏳ 待批准（v2，2026-09-22 按用户分支纪律修订）
```

# 落地《断流协议》四角色提示词

## 目标

把《断流协议：独立开发 AI 协作 Agent》四段提示词落成本仓的常驻入口与可复制文件，使「受控变更、可回滚、可审计」成为默认流程：

1. 任何需求 → 先出 Spec（含 ID/级别/文件范围/测试/回滚）→ 等用户 `批准 SPEC-<ID>` → 才动文件；
2. agent 只能改 Spec 列出的文件；越界即停下发 RCR；
3. 每次执行完按固定格式交付报告（分支/文件/测试/风险/回滚/下一步）。

## 非目标

- 不改任何 `.py`、不改 `data/`、不改 `.env`、不动依赖
- 不建 CI / pre-commit（属债务整改 M7，另立 SPEC）
- 不重写 `docs/工程惯例.md`（既有真源：AGENTS.md 只做入口与硬约束，细节指向它，避免再造第三份惯例文档）
- 不追溯改写历史提交
- 不把 SPEC 台账迁走（台账属 SPEC-20260922-03，已落地）

## 需要读取的文件

- @docs/工程惯例.md（避免重复真源）
- @docs/architecture/逆向架构Spec_V16_2026-09-22.md（债务台账 A1–A9 将被 AGENTS.md 引用）
- @docs/specs/INDEX.md（分支纪律、RCR 模板已在此定义，AGENTS.md 直接引用）
- @CHANGELOG.md（版本节奏约定）

## 新增文件

| # | 路径 | 内容 |
|---|---|---|
| 1 | `AGENTS.md`（仓库根） | 第 1 段《断流协议》**逐字保留**（身份/三条硬红线/改动分级 L0–L3/模式切换 `/spec` `/code` `/audit` `/hotspot`/Spec 必含字段/执行规则/完成报告格式）+ 新增「rezero_twin 项目适配」段 |
| 2 | `docs/agents/SPEC_AGENT.md` | 第 2 段**逐字** + SPEC 落盘位置（`docs/specs/`，并更新 `INDEX.md` 状态） |
| 3 | `docs/agents/CODE_AGENT.md` | 第 3 段**逐字** + 本仓分支/验证命令/收工四件套适配 |
| 4 | `docs/agents/AUDIT_AGENT.md` | 第 4 段**逐字**（热点表格字段 + 单文件审计清单 + 小步重构计划要求） |

## 「项目适配」段必须钉住的内容（本仓真实口径）

1. **分支纪律**：引用 `docs/specs/INDEX.md`——一 Spec 一分支、活跃 ≤2、寿命 ≤1 天（最长 1 周）、squash merge、合并后立即删本地+远程分支、每周 5 分钟清理。
2. **四条验证命令**（全部 `env -u PYTHONPATH` 前缀，防宿主 PYTHONPATH 污染）：
   - `env -u PYTHONPATH venv/Scripts/python.exe -m pytest tests/ -q`（当前基线 **294 passed**，零 API）
   - `env -u PYTHONPATH venv/Scripts/python.exe tests/smoke_test.py`（当前 **31/31**）
   - 打包：`env -u PYTHONPATH powershell.exe -NoProfile -ExecutionPolicy Bypass -File build.ps1`
   - 隔离校验：跑测试前后对 `data/memory.json{,bak} data/gui.log data/life.db data/conversations.db data/vignette_cache.json` 做 `stat -c '%n %Y %s'` 快照 `diff`
3. **「不改变文件的命令」在本仓的操作定义** = 跑完测试后 `data/` 逐字节不变（mtime+size）。
4. **真机确认门禁**：观感类（视觉/动效/间距）与行为类（数值刷新时机/交互反馈）改动，必须由用户在打包 EXE 上确认后才 merge。
5. **收工四件套**：CHANGELOG + devlog + 打包 EXE + 冒烟回归（缺一不可）。
6. **SPEC 文件免二次批准规则**：`docs/specs/**` 与 `CHANGELOG.md`、`docs/devlog/**` 的写入属常规产物（L0），但对代码文件的写入**只**允许被批准 SPEC 列出的范围。
7. **报告格式**：分支 / 修改文件 / 新增文件 / 提交记录 / 测试结果 / 未运行测试及原因 / 风险 / 回滚方式 / 下一步建议。
8. **历史债务入口**：指向 `docs/architecture/逆向架构Spec_V16_2026-09-22.md` 的 A1–A9 台账（含已修标记）。

## 接口/数据是否变化

无。无 API 变更、无数据结构变更、无持久化变更、无依赖变更。

## 测试方式

无代码测试。验证 = ① `git status --short` 只显示新增文档；② 与用户四段原文**逐段核对**无删改；③ 抽查 AGENTS.md 里的四条命令能实跑通过。

## 回滚方式

删除这 4 个文件（`git revert <该提交>`）；纯新增且无代码引用，回滚零副作用。

## 风险

- **低**。唯一实质风险是「文档写的流程与真实操作不一致」→ 用「项目适配」段把真实命令、`PYTHONPATH` 坑、测试隔离口径、真机门禁逐条钉死。
- 次要风险：与 `docs/工程惯例.md` 分裂 → 已列为非目标 + 用指针解决。

## 修订

- v1（2026-09-22）：初版（5 文件，含 `docs/specs/INDEX.md`）。
- v2（2026-09-22）：`INDEX.md` 移交 SPEC-20260922-03（已落地）；按用户新定稿补入分支纪律（一 Spec 一分支 / squash merge / 合并即删分支）与 `docs/` 前缀分支命名。

```
等待批准：是。请回复：批准 L0 SPEC-20260922-01
```
