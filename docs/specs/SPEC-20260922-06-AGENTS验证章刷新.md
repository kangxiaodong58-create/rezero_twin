```
SPEC-ID：SPEC-20260922-06
级别：L0（纯文档）
状态：✅ 已完成（用户批准）
分支：docs/SPEC-20260922-06-agents-verification-refresh（已删除）
```

# AGENTS.md 验证章刷新（基线 + 隔离语义 + CI 事实）

## 目标

把 `AGENTS.md`「rezero_twin 项目适配」段里**已过时的三处事实**对齐到 V16.3.3 现状：

1. §2 全量测试基线 `294 passed` → **`295 passed`**；
2. §2「测试隔离校验」行：改为对 `data/` **全部文件**快照（不再只列六个文件名），并指向 `scripts/verify_isolation.sh`；
3. §2 末句「本仓…无 CI」→ 改为「CI 门禁自 V16.3.0 起存在（`.github/workflows/ci.yml`）」；
4. §3「不改变文件的命令」：补 V16.3.3 的隔离语义（conftest autouse 夹具已全局重定向 `get_data_dir()`，新测试无需手工补丁；机械判据 = 干净 checkout 跑完后 `find data -type f` 为空）。

## 非目标

- 不改任何代码 / 测试 / workflow；不改 SPEC 台账结构；不处理 A10、B1–B5 债务；不重打包 EXE（无代码变化）

## 需要读取/修改的文件

- @AGENTS.md（§2 表 2 处 + 表末句 1 处 + §3 一行语义）
- @docs/specs/INDEX.md（新增本 SPEC 行）
- @CHANGELOG.md（V16.3.4 条目）

## 新增文件

- `docs/specs/SPEC-20260922-06-AGENTS验证章刷新.md`（本文件）

## 接口/数据是否变化

无。

## 测试方式

无代码测试（纯文档）。验证 = ① `grep` 断言 AGENTS.md 含 `295 passed`、含 `verify_isolation.sh`、含 `.github/workflows/ci.yml`，且**不再**含 `294 passed`；② `pytest tests/ -q` 仍 `295 passed`（确认无意外代码改动）。

## 回滚方式

`git revert <该批次 squash 提交>`（纯文档，零运行时影响）。

## 风险

低。唯一风险是「当前基线」写死后再次漂移 → 已在 §2 表内注明「随版本更新，以最近报告 / CHANGELOG 为准」。

## 等待批准：已批准（用户 2026-09-22：「批准 L0 SPEC-20260922-06」）

## 执行记录（2026-09-22）

- 四处文字更新完成；`grep` 断言通过；`pytest tests/ -q` → `295 passed` 无回归。
- **未跑项及原因**：打包 EXE 与真机确认**不适用**——本 SPEC 无任何代码/行为变化（纯文档），无观感或交互改动可确认，也无需重打包。

## 修订

- v1（2026-09-22）：草案（随批准落地）。
